# Мониторинг температуры по барам (`/temperature`)

## Что это

Страница `/temperature` показывает текущую температуру и влажность воздуха в каждом
из 4 баров: в зале стоит умный термо-гигрометр (Tuya / Smart Life), приложение
опрашивает Tuya Cloud и привязывает показания к каноническим барам
(`bolshoy`/`ligovskiy`/`kremenchugskaya`/`varshavskaya`).

Зачем: видеть на одном экране, не жарко/холодно ли в зале, не отвалился ли датчик,
и динамику за сутки (спарклайн). Данные обновляются автоматически.

Источник прототипа — рабочий Jupyter-ноутбук владельца (Tuya OpenAPI v2.0). При
переносе в проект: секреты вынесены в `.env`, добавлены кэш токена, корректный
масштаб из спецификации устройства, история в SQLite и фоновый опрос.

## Файлы

| Файл | Роль |
|---|---|
| [core/tuya_temperature.py](../core/tuya_temperature.py) | Клиент Tuya: подпись, кэш токена, масштаб из спецификации, маппинг устройство->бар, диапазоны окраски |
| [core/temperature_store.py](../core/temperature_store.py) | История показаний в SQLite (`temperature.db`), дедуп по бакетам |
| [core/temperature_scheduler.py](../core/temperature_scheduler.py) | Daemon-поток фонового опроса каждые `TUYA_POLL_MINUTES` минут |
| [core/temperature_alarm.py](../core/temperature_alarm.py) | Тревога по высокой температуре: порог + гистерезис + отправка в чаты тревог бота |
| [routes/temperature.py](../routes/temperature.py) | Страница `/temperature` + `GET /api/temperature/current` + `GET /api/temperature/history` |
| [templates/temperature.html](../templates/temperature.html) | UI: карточки 4 баров, влажность/батарея/связь, спарклайн, легенда, история (24ч/7д/14д), автообновление |
| `.env` / `.env.example` | `TUYA_ACCESS_ID/SECRET/ENDPOINT/POLL_MINUTES/DEVICE_MAP` |
| [routes/\_\_init\_\_.py](../routes/__init__.py), [app.py](../app.py), [templates/shared/nav.html](../templates/shared/nav.html) | Регистрация blueprint, старт шедулера, пункт меню |

## Как работает

### 1. Авторизация и подпись (Tuya OpenAPI v2.0)

Каждый запрос подписывается HMAC-SHA256:

```
stringToSign = METHOD + "\n" + sha256(body) + "\n\n" + path
sign         = HMAC_SHA256(secret, accessId + [accessToken] + t + stringToSign).hex().upper()
```

- `body` для GET пустой, `sha256("")` константа; `t` — текущее время в миллисекундах.
- Для запроса токена `accessToken` пустой; для остальных — подставляется токен.
- Токен (`POST /v1.0/token?grant_type=1`) кэшируется в памяти процесса до
  `expire_time - 60 c`. При ответе «token invalid» (код 1010-1012) токен один раз
  принудительно обновляется и запрос повторяется.

### 2. Маппинг устройство -> бар

Константа `DEVICE_TO_VENUE` в `core/tuya_temperature.py`. Три бара однозначны
(`varsh`/`lig`/`krem`), четвёртый по элиминации: `Andr` (Андреевский, Васильевский
остров) = `bolshoy` (Большой пр. В.О). Переопределяется через env `TUYA_DEVICE_MAP`
(JSON `{"<device_id>": "<venue_key>"}`) без правок кода.

### 3. Масштаб показаний (важно для корректности)

Датчик отдаёт значение целым числом; реальное значение = `raw / 10**scale`, где
`scale` берётся **из спецификации устройства** (`GET /v1.0/devices/{id}/specifications`,
поле `status[].values.scale`), а не угадывается по величине.

- Температура: `va_temperature`, `scale=1` => `236 -> 23.6 C`.
- Влажность: `va_humidity`, `scale=0` => `62 -> 62 %` (не делим).

Почему не эвристика «если raw > 100, делить на 10»: на холоде `95` при `scale=1` —
это `9.5 C`, а эвристика показала бы `95 C`. Спецификация кэшируется на весь процесс
(не меняется). Фоллбэк, если спецификация недоступна: температура `scale=1`,
влажность/батарея `scale=0`.

### 4. Диапазоны окраски (`TEMP_BANDS`)

Единый источник правды — константа `TEMP_BANDS`; UI берёт её из API
(`meta.bands`), поэтому цвет карточки и легенда всегда совпадают с кодом. Это
температура **воздуха в зале**, границы — ориентир комфорта, не жёсткая норма:

| Диапазон | °C | Цвет |
|---|---|---|
| Холодно | < 16 | синий |
| Норма | 16–25 | зелёный |
| Тепло | 25–28 | жёлтый |
| Жарко | > 28 | красный |

### 5. История и дедуп под 2 воркера

Показания пишутся в `temperature.db` (SQLite, WAL) как **сэмпл на каждый опрос**.
Первичный ключ `(bar_key, bucket_ts)`, где `bucket_ts = (sample_ts // interval_s) * interval_s`
— **время опроса** (wall-clock), округлённое вниз до интервала.

Важно: бакетируем по времени ОПРОСА, а не по `update_time` устройства. Датчики Tuya
шлют данные по изменению температуры, и при стабильности их `update_time` стоит часами —
если бакетировать по нему, история была бы почти пустой (1-2 точки за неделю). Сэмпл на
каждый опрос (раз в 15 мин) даёт ровный лог последнего известного значения. На карточке
«Датчик: N назад» — это отдельное, живое `update_time` (когда датчик сам прислал данные),
оно в историю не пишется.

Под `gunicorn --workers 2` оба воркера опрашивают независимо, но `INSERT OR IGNORE`
на один бакет оставит ровно одну строку. Это проще и надёжнее lock-файлов (ср.
`core/open_check_scheduler.py`): если у одного воркера запрос к Tuya упал, значение
запишет другой.

Путь к БД: `/kultura/temperature.db` на проде (persistent volume), `data/temperature.db`
локально (в `.gitignore`). Старше 90 дней подчищается (`prune`).

### 6. Фоновый опрос

`core/temperature_scheduler.py` — daemon-поток. Стартует из `app.py` только если
заданы `TUYA_ACCESS_ID/SECRET` и `TUYA_POLL_MINUTES > 0` (иначе фича тихо выключена).
Делает стартовый опрос сразу, далее каждые `TUYA_POLL_MINUTES` минут; раз в сутки
чистит историю.

### 7. API

| Endpoint | Описание |
|---|---|
| `GET /api/temperature/current` | Текущие показания. Живой опрос Tuya с кэшем 60 с; `?force=1` обходит кэш. Живой опрос пополняет историю. Если Tuya недоступна/не настроена — фоллбэк на последнее из БД (`source: store`). Ответ: `{bars:[...], meta:{bands, configured, poll_minutes}, updated_at, source}` |
| `GET /api/temperature/history?hours=24` | Точки за период (`hours` 1–720, до 30 дней) для спарклайна и истории: `{bars:{venue_key:[{ts,temperature,humidity}]}}`. `max_points` (по умолч. 1500) прорежает точки на длинных периодах (равномерно, первая/последняя сохраняются) |

### 8. UI

Карточки 4 баров: крупная температура с окраской по диапазону, метка диапазона,
влажность, батарея (если датчик отдаёт; подсветка при ≤ 20 %), спарклайн за 24 ч,
время последнего показания и бейдж связи. Бейдж берётся из `online` Tuya
(`на связи` / `не на связи`): датчики шлют значение при ИЗМЕНЕНИИ температуры, а не
постоянно, поэтому «возраст» показания в 1-2 ч при стабильной температуре — норма, а
не потеря связи (на возраст бейдж не завязан). `давно нет данных` — лишь запасной
сигнал, когда `online` неизвестен И показанию больше 3 ч. Кнопка «Обновить» (force) и
автообновление раз в 60 с. Легенда с диапазонами.

**Два разных времени** (частый вопрос): «Опрошено» сверху — когда страница в последний
раз запросила Tuya (по кнопке/автообновлению, обычно «только что»). «Датчик: N назад»
под баром — `update_time` устройства, т.е. когда сам датчик прислал показание; раз
датчики шлют по изменению, эти времена различаются и под баром нередко 1-2 ч. Под
тулбаром есть поясняющая подпись.

**История** — сворачиваемая секция «История температуры» под карточками: переключатель
периода (24 часа / 7 дней / 14 дней) и график-линия по каждому бару (мин/сред/макс за
период + даты концов). Строится из `/api/temperature/history`. Данные копятся с момента
запуска мониторинга, поэтому первые дни график короткий и заполняется со временем.

### 9. Тревога по высокой температуре

Если температура воздуха в баре поднялась выше `TUYA_ALARM_TEMP_C` (по умолчанию
**26 C**) — бот открытия баров (`@kultura_open_bot`) шлёт аларм в те же чаты, что и
тревоги о закрытых барах (`TELEGRAM_ALARM_CHAT_IDS` + самоподписавшиеся). Проверка идёт
в фоновом опросе (`core/temperature_scheduler.py`), поэтому нужен `TUYA_POLL_MINUTES > 0`.
На живом опросе страницы тревоги НЕ оцениваются — чтобы они шли по предсказуемому
расписанию, а не на каждое открытие `/temperature`.

Антиспам и логика (`core/temperature_alarm.py`):

- аларм шлётся **один раз** при пересечении порога снизу вверх (`t > 26`);
- пока бар «горит» — тишина (повторов каждые 15 мин нет);
- когда температура опускается ниже `TUYA_ALARM_CLEAR_C` (по умолчанию **25 C**) —
  шлём «вернулось в норму» и снимаем тревогу. Зазор 25..26 — гистерезис против дребезга
  у границы 26.0;
- состояние тревоги по бару — таблица `alarm_state` в `temperature.db`. Переход —
  **атомарный compare-and-set** (`set_alarm_state`), поэтому под `--workers 2` аларм
  шлёт ровно один воркер. Если отправка не удалась — состояние откатывается, и следующий
  опрос повторяет (тревога не «проглатывается»).

Сообщение (первые строки жирным/капсом — видны в пуш-уведомлении):

```
!!! ЖАРКО В БАРЕ !!!
ВО — 27.0 C
Порог 26 C. Опрос 17:16 МСК.
```

Порог тревоги (26) намеренно отделён от цветовых диапазонов карточек (`TEMP_BANDS`,
«жарко» = > 28): диапазоны — про комфорт на странице, порог — про оперативную тревогу.
На проде получатели уже заданы (`TELEGRAM_ALARM_CHAT_IDS`), отдельная настройка не нужна.

### Переменные окружения

```
TUYA_ACCESS_ID=...        # Cloud-проект на iot.tuya.com
TUYA_ACCESS_SECRET=...
TUYA_ENDPOINT=https://openapi.tuyaeu.com   # EU/РФ; US — tuyaus.com, CN — tuyacn.com
TUYA_POLL_MINUTES=15      # 0 = фоновый опрос выключен (живой опрос на странице остаётся)
TUYA_DEVICE_MAP=          # (опц.) JSON-переопределение маппинга устройство->бар
TUYA_ALARM_TEMP_C=26      # порог тревоги (выше -> аларм в чаты тревог бота)
TUYA_ALARM_CLEAR_C=25     # порог возврата в норму (гистерезис; по умолчанию порог - 1)
```

На проде (Selectel VPS) задать те же переменные в окружении контейнера; секреты —
в рунбуке `C:\KULT\ctr\selectel-migration-credentials.md`.

## Changelog

- **2026-06-29 (4)** — История температуры на странице: сворачиваемая секция с графиком
  по каждому бару и переключателем периода (24ч / 7д / 14д); `/api/temperature/history`
  расширен до 720 ч (30 дней) + `max_points` (прорежывание). Исправлена путаница со
  временем: «Обновлено» -> «Опрошено» (наш опрос), под баром явный префикс «Датчик:»
  (когда устройство само прислало данные) + поясняющая подпись под тулбаром.
- **2026-06-29 (3)** — Тревога по высокой температуре: если в баре `t > TUYA_ALARM_TEMP_C`
  (26 C) — аларм в чаты тревог open-check бота. Антиспам через состояние `alarm_state`
  (один аларм на пересечение порога, возврат «в норму» ниже 25 C — гистерезис), дедуп
  под 2 воркера атомарным compare-and-set, откат при неудачной отправке. Новый
  `core/temperature_alarm.py`, проверка в фоновом опросе. Тест из 13 проверок (smoke).
- **2026-06-29 (2)** — Команда `/temp` в open-check боте (живой опрос из чата) + фикс
  бейджа связи на карточках: «давно» гасло на здоровых датчиках (порог был привязан к
  интервалу опроса), теперь признак — `online` от Tuya. См. [open-check-bot.md](open-check-bot.md).
- **2026-06-29** — Перенос мониторинга температуры из Jupyter-прототипа в приложение:
  страница `/temperature`, клиент Tuya с кэшем токена и масштабом из спецификации
  (фикс эвристики «>100» — корректно на холоде), история в SQLite с дедупом по
  бакетам, фоновый опрос каждые 15 мин, секреты в `.env`. Проверено на живом API:
  4 датчика отдают температуру/влажность (23–24 °C, 44–62 %), `va_temperature` scale=1,
  `va_humidity` scale=0.
