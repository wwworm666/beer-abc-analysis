# Shelf-Life Cockpit — страница `/expiration`

## Что это

Отдельная страница для управления сроками годности фасовки на основе данных
ЧЗ + iiko. Превращает «список с подсветкой» (вкладка «Сроки годности» в `/stocks`)
в action layer:

1. KPI-полоса (3 метрики: ₽ под риском, критичных SKU, излишек шт.)
2. Светофор-вкладки по tier-у срочности (expired / critical / urgent / watch / fresh / unknown)
3. Карточный grid с tier-меткой, метрикой дней до истечения, остатком, расходом, ценой
4. Рекомендация: уценка по tier-ставке ИЛИ перевод в другой бар где velocity выше
5. Режим «Актив-лист» — упрощённый печатный вид для барменов (см. ниже)

Старый таб «Сроки годности» в `/stocks` остаётся как «view-only». Cockpit — отдельная страница.

## Файлы

- `routes/expiration.py` — Blueprint: `GET /expiration` (страница), `GET /api/expiration/board` (данные)
- `core/expiry_recommend.py` — `classify_tier()`, `recommend()`, константы (PRICE_ELASTICITY, tier-пороги)
- `templates/expiration.html` — UI: KPI + tier-tabs + cards-grid
- `routes/__init__.py` — регистрация `expiration_bp`
- `templates/shared/nav.html` — пункт меню «Сроки годности»

## Как работает

### Источники данных

- **iiko OLAP `/balances`** — текущие остатки по складам (товар → store_id → amount, sum)
- **iiko OLAP `/operations`** за последние 30 дней — расход (для расчёта velocity = avg_sales/день)
- **iiko nomenclature** — name, category (поставщик)
- **Цена** = `sum / amount` из balances — это **себестоимость единицы** (cost basis),
  а не цена продажи (sale price отсутствует в nomenclature). Используем cost basis
  как честную базу для оценки риска списания: сколько денег уйдёт «в минус»
  при списании партии.
- **ЧЗ-кэш** `chz_test/debug/chz_stock.json` — партии с production_date, expiration_date,
  привязанные к КПП конкретного бара (поле `by_kpp`)

Стыковка iiko↔ЧЗ через barcode (EAN-13) ↔ gtin (GTIN-14, lpad'0').

### Откуда берётся `chz_stock.json` (с 2026-06 — важно)

С июня 2026 общепит по послаблению ГИС МТ **выводит коды из оборота сразу при
приёмке** (status `RETIRED`, причина `OWN_USE`/`PRODUCTION_USE` — чтобы продавать
бутылки с повреждённым Data Matrix). Последствия:

- старый фильтр `status=INTRODUCED` больше **не отдаёт остаток** (коды мгновенно RETIRED);
- асинхронная dispenser-выгрузка (`chz.py csv-auto`) по RETIRED **виснет** в статусе
  PREPARATION часами — мёртвый путь.

Поэтому `chz_stock.json` теперь собирается **синхронным `/cises/search`**
(`chz.py search-stock` → `get_chz_stock_via_search`), причём **точечно по списку GTIN**:

- **Почему точечно:** `/cises/search` по `ownerInn` без GTIN-фильтра отдаёт коды
  newest-first и захлёбывается ~20 высокообъёмными GTIN (каждый — тысячи кодов),
  длинный хвост каталога (сотни SKU) недостижим даже без окна по дате. Поэтому
  спрашиваем ЧЗ **ровно про те GTIN, что бар держит на остатке в iiko**.
- **Список нужных GTIN** считает прод (`core/chz_needed_gtins.compute_needed_gtins`:
  фасовка in-stock из iiko balances → баркоды) и передаёт на бар-ПК
  (`needed_gtins.json`). `remote_exec run search-stock` это оркеструет.
- **Запрос:** на каждый GTIN — `/cises/search` с фильтром `gtins=[g]` (единственный
  фильтр, который API уважает; `kpp`/`modId` игнорируются), 1–3 страницы.
- **Привязка к бару** — по `modId` кода → `ownerMod.kpp` через `/cises/info`
  (НЕ `kpp`/`applicationMod` — это поставщик); modId резолвится лениво (4 бара).
- Если списка GTIN нет (дев/нет iiko) — fallback в «брод»-режим (окно
  `SEARCH_STOCK_WINDOW_DAYS`, early-stop по сходимости) — покрывает лишь
  высокообъёмные GTIN, для отладки.
- Формат `chz_stock.json` (с `by_kpp`) сохранён → роуты не меняются.

**Покрытие:** ~70% in-stock GTIN находятся в ЧЗ (из них ~84% с привязкой к бару).
Остальные ~30% — **импорт** (иностранные GTIN `050…`/`0541…`, маркируются
импортёром) и кривые/внутренние баркоды — их в ЧЗ под нашим ИНН нет вообще
(подтверждено: пусто даже без `ownerInn`). Это потолок, не баг.

**Важно про смысл данных:** «вывод из оборота» теперь = «приёмка», поэтому ЧЗ больше
НЕ отражает «что физически на полке». **Остаток берём только из iiko**, а ЧЗ — лишь
источник **сроков годности по партиям**. Подробности и сетап: см. `docs/chz-stock-integration.md`.

### Tier-классификация (core/expiry_recommend.py)

| Tier | days_to_expiry | Базовая скидка |
|------|----------------|----------------|
| `expired`  | < 0     | (списание) |
| `critical` | 0..7    | 35% |
| `urgent`   | 8..14   | 20% |
| `watch`    | 15..30  | 10% |
| `fresh`    | > 30    | 0% |
| `unknown`  | None    | (нет данных ЧЗ) |

### Формулы

```
velocity        = outgoing_30d / 30                  # шт/день
expected_sales  = velocity × days_to_expiry          # успеем продать без вмешательства
surplus         = max(0, stock − expected_sales)     # излишек

# Уценка (если surplus > 0)
boosted_velocity = velocity × (1 + PRICE_ELASTICITY × discount/100)
boosted_sales    = boosted_velocity × days
expected_writeoff = max(0, stock − boosted_sales)

# Risk
risk_rub = surplus × price                           # ₽ ожидаемого списания
```

`PRICE_ELASTICITY = 1.5` — эмпирическая константа эластичности спроса по цене для пива.
Пример: −20% цены → +30% спроса.

### Перевод в другой бар

Кандидат подходит, если у него:
- тот же `product_id`,
- `velocity × days_to_expiry ≥ surplus` (успеют продать наш излишек),
- `stock_level ∈ {low, medium}` (низкий/средний остаток, есть «куда положить»).

Перебираем кандидатов в порядке raw, берём первого подходящего.

### KPI

- **Под риском, ₽** = Σ(risk_rub) по всем не-fresh не-unknown позициям
- **Критично сейчас** = count(tier ∈ {expired, critical})
- **Не успеваем продать** = Σ(surplus) по всем

### Производительность

- `get_store_balances()` вызывается **1 раз** на запрос (раньше N раз по числу баров),
  фильтрация по `store` идёт уже на уровне `_build_bar_data()`
- Запросы `/operations` по барам выполняются **параллельно** через `ThreadPoolExecutor(4)`
- Ответ **кешируется в памяти на 120 секунд** (`_BOARD_CACHE`, `threading.Lock`).
  Force-refresh — `?force=1`. Кеш у каждого gunicorn worker свой
- Метрики времени отдаются клиенту в `cache: {hit, age_sec}`, UI показывает бейдж

### Режим «Актив-лист» (для барменов)

Переключатель «Cockpit / Актив-лист» в тулбаре страницы. Cockpit — менеджерский вид
(KPI, tier-вкладки, карточки); Актив-лист — рабочий инструмент бармена. Реализован
целиком на клиенте (`templates/expiration.html`), данные — тот же ответ
`/api/expiration/board`, бэкенд не менялся.

В режиме «Актив-лист» KPI и tier-вкладки скрыты, вместо карточек — две таблицы:

1. **«Горящие сроки (до 30 дней)»** — позиции с tier `expired|critical|urgent|watch`
   (константа `HOT_TIERS`), отсортированные по `days_to_expiry` по возрастанию.
   Колонки: № / Название / Бар (только при «Все бары») / Ближайший срок /
   Осталось (цвет по tier) / Остаток / Действие (первая опция рекомендации,
   кратко: «Списать N шт.», «Уценка −X%», «Передать N шт. → бар»).
2. **«Нет данных ЧЗ — проставить сроки вручную»** — позиции с tier `unknown`.
   Колонки: № / Название / Бар / Остаток по iiko + пустые графы
   «Срок годности», «Кол-во с этим сроком», «Примечание» — бармен распечатывает
   лист и вписывает сроки с этикеток ручкой.

**Печать:** кнопка «Печать» (видна только в этом режиме) вызывает `window.print()`;
`@media print` скрывает навигацию/тулбар/KPI, добавляет print-шапку (бар, дата печати,
дата данных ЧЗ), рисует таблицы чёрной сеткой без заливки (ч/б принтер), вторая
таблица начинается с новой страницы, строки не рвутся (`page-break-inside: avoid`),
пустые графы — 32pt высотой (~1.1 см) под запись ручкой.

### Endpoint `GET /api/expiration/board?bars=…`

Параметры:
- `bars=all` — все 4 бара (default)
- `bars=bar1,bar2` — выбранные
- `force=1` — игнорировать кеш

Ответ:
```json
{
  "updated_at": "2026-04-30T...",
  "chz_updated_at": "2026-04-29T...",
  "bars": ["Большой пр. В.О", "Лиговский", ...],
  "kpi": {"risk_rub": 124300, "critical_count": 12, "surplus_units": 87.5},
  "tier_counts": {"expired": 3, "critical": 12, "urgent": 28, ...},
  "cache": {"hit": false, "age_sec": 0},
  "items": [...]
}
```

## Известные ограничения

1. **Цена** = себестоимость (cost basis) `sum/amount` из balances, не цена продажи. Это занижает «упущенную выручку», но даёт честную оценку прямого финансового убытка от списания. Если `sum=0` (товар без проводок) — `risk_rub = 0`.
2. **Перевод** — только подсказка, фактическое перемещение не интегрировано (логистика руками).
3. **Журнал действий** не ведётся (MVP). Кнопок «применить» нет — только информация.
4. **PRICE_ELASTICITY** одинаков для всех категорий. Для крафта реальное значение ниже, для масс-маркета выше.
5. **Кандидат на перевод** — берётся первый подходящий, не оптимизируем по «куда выгоднее».
6. **ЧЗ ≠ остаток (с 2026-06).** Коды выводятся из оборота при приёмке, поэтому ЧЗ
   отражает «что приняли», а не «что на полке». Сроки годности берём из приёмок за
   последние 60 дней; редкая старая малотиражная партия, ещё лежащая на полке, в окно
   может не попасть — у такой позиции просто не будет срока из ЧЗ (остаток из iiko
   при этом виден). Для быстрого оборота бара это покрывает подавляющее большинство.
7. **Разбивка по барам — только пиво (beer).** В `/cises/search` пиво несёт `modId`
   места (→ КПП бара через `/cises/info`), а nabeer/softdrinks приходят с `modId`
   `None`/`0` и пустым `ownerMod` (часть — вообще `RETAIL`-выбытие). Поэтому
   безалкогольное пиво и софт-дринки попадают в слот `_unknown` и видны только в режиме
   «Общая» (по юрлицу), без разбивки по барам. Пива — основной массы фасовки — это не
   касается, у него привязка к 4 барам полная.

## Changelog

### 2026-07-11 — Актив-лист для барменов

**Что:** режим «Актив-лист» на /expiration — переключатель в тулбаре, две таблицы
(горящие до 30 дней по возрастанию срока; без данных ЧЗ с пустыми графами под
рукописные сроки), кнопка «Печать» + print-CSS (шапка, ч/б сетка, разрыв страниц).

**Почему:** Cockpit сделан под управляющего; барменам нужен простой список «что
горит» и печатный бланк для ручной сверки сроков позиций, которых нет в ЧЗ
(~30% фасовки — импорт и кривые баркоды).

**Файлы:** только `templates/expiration.html` (клиентский рендер поверх того же
`/api/expiration/board`), `docs/expiration.md`.

### 2026-06-26 — Переход на /cises/search (ЧЗ выводит коды при приёмке)

**Проблема:** все позиции на странице стали «нет в ЧЗ». Причина — бухгалтерия (по
послаблению ГИС МТ для общепита) выводит маркировку из оборота сразу при приёмке
(`RETIRED`/`OWN_USE`). Старая выгрузка тянула только `INTRODUCED` → пусто; dispenser
по `RETIRED` виснет.

**Что:**
- Новый источник `chz_stock.json` — синхронный `/cises/search` (`chz.py search-stock`,
  функции `get_chz_stock_via_search` + `resolve_mod_kpp`), формат с `by_kpp` сохранён.
- Привязка к бару — по `modId` → `ownerMod.kpp` (через `/cises/info`), т.к. в этом API
  нет `kpp`. Карта 4 баров проверена вживую.
- Early-stop по сходимости партий (пул кодов 100k+ → ~2-4k).
- `remote_exec.py` — обработчик `run search-stock`; `/api/chz/refresh` (routes/stocks.py)
  переключён с `csv-auto` на `search-stock`. Роуты `/expiration` и `/stocks` НЕ менялись.

**Файлы:** `chz_test/chz.py`, `remote_exec.py`, `routes/stocks.py`, `docs/expiration.md`,
`docs/chz-stock-integration.md`.

### 2026-04-30 — Оптимизация и удаление календаря

**Что:**
- Цена позиции = себестоимость `sum/amount` из balances (раньше price=0,
  потому что nomenclature не отдаёт sale price). KPI risk_rub теперь работает.
- Удалён календарь риска (визуальный шум, дублировал KPI).
- Балансы — 1 запрос вместо N (фильтр по `store` внутри `_build_bar_data()`).
- TTL-кеш ответа /api/expiration/board на 120с (`?force=1` для обхода).
- Параллельные `/operations` через `ThreadPoolExecutor(4)`.
- UI loader с 3 стадиями + бейдж времени/кеша в meta-row.

**Замер:** холодный 8с, тёплый 0.23с (×35).

### 2026-04-30 — Создание Shelf-Life Cockpit

**Что:** Новая страница `/expiration` с tier-классификацией, рекомендациями и календарём риска.

**Почему:** Текущий таб в `/stocks` пассивный — оператор видит «истекает через 6 дней» и сам считает скидку. Cockpit добавляет action layer.

**Файлы:**
- `routes/expiration.py` (новый)
- `core/expiry_recommend.py` (новый)
- `templates/expiration.html` (новый)
- `routes/__init__.py` (регистрация)
- `templates/shared/nav.html` (пункт меню)

**Не трогали:** существующий `/stocks` таб «Сроки годности».
