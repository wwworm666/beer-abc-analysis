# Настройка экспорта ЗП в Google Таблицу

## Что это

Расчёт ЗП попадает в **Google Таблицу бухгалтерии** — теми же строками и
формулами, что и `.xlsx`. Отличие от Excel-выгрузки: Таблицы считают формулы на
сервере, поэтому цифры видны сразу, без «пустых ячеек в предпросмотре».

Два пути, оба пишут **одну и ту же вкладку** «Июль_2026_Автоматическая»:
ночная выгрузка в 04:00 (сама) и кнопка **«Обновить Google»** на `/salary`
(сейчас, за открытый месяц).

Код готов и задеплоен. Чтобы кнопка заработала, нужен **ключ сервис-аккаунта
Google** — это единственный шаг, который делается руками в Google Cloud
Console. API-ключа недостаточно: запись в Таблицы требует учётной записи, а
сервис-аккаунт — единственный вариант без интерактивного входа человека.

Пока ключа нет, кнопка отвечает «Экспорт в Google Таблицу не настроен» и
ничего не ломает — `.xlsx` работает как работал.

---

## Часть 1. Google Cloud Console (~5 минут, делается один раз)

### 1. Создать проект

1. Открыть <https://console.cloud.google.com/>
2. Вверху слева выпадающий список проектов -> **Создать проект**
3. Имя: `beer-kultura` (любое) -> **Создать**
4. Дождаться создания и **выбрать** этот проект в том же списке

### 2. Включить Sheets API и Drive API

1. Меню -> **API и сервисы** -> **Библиотека**
2. Найти **Google Sheets API** -> **Включить**
3. Вернуться в Библиотеку, найти **Google Drive API** -> **Включить**
   (нужен, только если экспорт будет создавать новые таблицы; если пишем в
   существующую — можно пропустить)

### 3. Создать сервис-аккаунт

1. Меню -> **API и сервисы** -> **Учётные данные**
2. **Создать учётные данные** -> **Сервисный аккаунт**
3. Имя: `beer-salary-export` -> **Создать и продолжить**
4. Роль не выбирать (права на таблицу выдаются шарингом, а не ролью IAM) ->
   **Продолжить** -> **Готово**

### 4. Скачать JSON-ключ

1. В списке учётных данных нажать на созданный сервис-аккаунт
2. Вкладка **Ключи** -> **Добавить ключ** -> **Создать ключ** -> **JSON** ->
   **Создать**
3. Файл скачается сам. Это и есть ключ — **никому не пересылать и не
   коммитить в git**
4. Там же скопировать **email сервис-аккаунта** — вида
   `beer-salary-export@beer-kultura.iam.gserviceaccount.com`

### 5. Расшарить таблицу бухгалтерии

1. Открыть таблицу, в которую должны попадать вкладки месяцев
2. **Настройки доступа** -> вставить email сервис-аккаунта из шага 4
3. Права — **Редактор** -> **Отправить**
   (галочку «Уведомить» можно снять — это робот)
4. Скопировать **id таблицы** из адресной строки — длинная строка между
   `/d/` и `/edit`:
   `https://docs.google.com/spreadsheets/d/`**`1AbC...XyZ`**`/edit`

---

## Часть 2. Сервер (делается один раз)

Положить ключ на VPS и прописать две переменные:

```bash
# 1. Скопировать скачанный JSON на сервер (выполнять на своём компьютере)
scp ~/Downloads/beer-kultura-*.json root@139.100.200.92:/srv/beer/secrets/google-sa.json

# 2. Дальше — на сервере
ssh root@139.100.200.92
mkdir -p /srv/beer/secrets && chmod 700 /srv/beer/secrets
chmod 600 /srv/beer/secrets/google-sa.json

# 3. Прописать переменные окружения
cd /opt/beer
echo 'GOOGLE_SA_JSON=/app/secrets/google-sa.json' >> .env
echo 'SALARY_SHEET_ID=<id таблицы из шага 5>'      >> .env

# 4. Примонтировать папку с ключом в контейнер: в docker-compose.yml
#    в секцию volumes сервиса app добавить строку
#      - /srv/beer/secrets:/app/secrets:ro

# 5. Перезапустить
docker compose up -d --build app
```

Проверка, что контейнер видит ключ:

```bash
docker exec beer-app python -c "from core.salary_gsheet import _load_credentials; \
  print('сервис-аккаунт:', _load_credentials().service_account_email)"
```

---

## Как пользоваться

### Кнопка — обновить сейчас, не дожидаясь ночи

1. `/salary` -> выбрать месяц -> **Рассчитать**
2. **Обновить Google** -> вкладка «Июль_2026_Автоматическая» в таблице
   `SALARY_SHEET_ID` перезаписывается расчётом с экрана, таблица открывается

Ручная вкладка бухгалтера («июль2026») не трогается — пишется только
«..._Автоматическая», как и ночью.

> **Почему кнопка не создаёт отдельный файл.** Создавать файлы сервис-аккаунту
> недоступно: `POST /v4/spreadsheets` отвечает `403 The caller does not have
> permission` (у него нет своего Диска). Так было в проде 2026-08-05. Запись в
> расшаренную таблицу работает, поэтому кнопка делает то же, что ночная задача.
> Если `SALARY_SHEET_ID` не задан, кнопка попробует создать новую таблицу
> (фоллбэк) — и упадёт с понятной подсказкой, если прав нет. Тогда шаринг новых
> файлов настраивается через `SALARY_SHARE_WITH`.

### Автоматически — в таблицу бухгалтерии

Каждую ночь в **04:00** приложение само собирает расчёт и переписывает в
таблице `SALARY_SHEET_ID` вкладку **«Июль_2026_Автоматическая»**.

- Выгружается **текущий месяц** — вкладка появляется 1-го числа и наполняется
  каждую ночь. В первые дни продаж ещё нет, поэтому премии нулевые, а часы и
  смены уже идут из графика.
- **Предыдущий месяц** обновляется ещё неделю (до 7 числа) — чтобы поздние
  правки графика и кассы попали в закрытый месяц.
- **Ручная вкладка месяца («июль2026») не трогается.** Ночная задача
  переписывает только свой лист, поэтому «мосты», «Отпуск», «Доп доход» и
  вычеты, которые вы вносите руками, в безопасности.
- Прогнать сейчас, не дожидаясь ночи:
  `curl -X POST http://127.0.0.1:10000/api/salary/sync-gsheet` на сервере, или
  `docker exec beer-app python -c "from core.salary_scheduler import sync_once; print(sync_once())"`.

> **Вкладка — снимок, а не живой вид расчёта.** Она меняется только в эти три
> момента: ночью в 04:00, по кнопке и по командам выше. Деплой её НЕ обновляет,
> поэтому после правки, меняющей содержимое листа, вкладку надо перезаписать —
> иначе в таблице ещё сутки будет старый вид (так вышло 2026-08-06 с алфавитным
> порядком колонок). Проверить, что реально лежит в листе, можно чтением шапки:
> `values().get(range="'Июль_2026_Автоматическая'!A2:BZ2")` — сотрудники идут
> с колонки F.

Оба сценария пишутся в журнал графика (`salary_gsheet_export`,
`salary_gsheet_sync`) — видно, кто и когда выгружал.

---

## Переменные окружения

| Переменная | Обязательна | Смысл |
|---|---|---|
| `GOOGLE_SA_JSON` | да | Путь к JSON-ключу в контейнере (по умолчанию `/app/secrets/google-sa.json`) |
| `GOOGLE_SA_JSON_CONTENT` | нет | Ключ строкой вместо файла (альтернатива монтированию) |
| `SALARY_SHEET_ID` | для ночной | id таблицы бухгалтерии — цель автоматической выгрузки. Не задан — планировщик не стартует, кнопка работает |
| `SALARY_SHARE_WITH` | нет | Email через запятую: кому шарить таблицы, созданные кнопкой. Задан — доступ по ссылке не включается |
| `SALARY_SYNC_HOUR` / `SALARY_SYNC_MINUTE` | нет | Время ночной выгрузки (по умолчанию 04:00) |
| `SALARY_SYNC_PREV_UNTIL_DAY` | нет | До какого числа обновлять предыдущий месяц (по умолчанию 7) |
| `SALARY_SYNC_ENABLED` | нет | `0` — выключить ночную выгрузку |
| `SALARY_SHEET_FOLDER_ID` | нет | Папка Drive для новых таблиц (работает, только если папку создал сам сервис-аккаунт — скоуп `drive.file`) |

---

## Если что-то не работает

| Сообщение | Причина | Что делать |
|---|---|---|
| «Экспорт в Google Таблицу не настроен» / «Нет ключа сервис-аккаунта» | Ключа нет по пути или папка не примонтирована | Часть 2, шаги 1–4 |
| «Не установлены библиотеки Google API» | Образ собран до появления зависимостей | `docker compose up -d --build app` |
| `SERVICE_DISABLED` / «Google Sheets API has not been used in project ...» | В проекте не включён Sheets API. Частый случай: проект заведён автоматически под Gemini/AI Studio (имя вида `gen-lang-client-…`) — там включён только Generative Language API | Открыть ссылку из текста ошибки (`console.developers.google.com/apis/api/sheets.googleapis.com/overview?project=<номер>`) -> **Включить**, подождать 1–2 минуты |
| «Таблица ... недоступна» | Таблица не расшарена на сервис-аккаунт | Часть 1, шаг 5, права **Редактор** |
| `403 The caller does not have permission` при создании таблицы | Сервис-аккаунту нельзя создавать файлы (нет своего Диска) | Задать `SALARY_SHEET_ID` — таблицу бухгалтерии, расшаренную на сервис-аккаунт; тогда и кнопка, и ночная выгрузка пишут в неё |
| «Таблица недоступна» у ночной выгрузки | `SALARY_SHEET_ID` задан, но таблица не расшарена | Часть 1, шаг 5, права **Редактор** |

Доступность Google API с прода проверена 2026-08-01 (ТСПУ его не режет, в
отличие от Telegram — см. `docs/open-check-bot.md`).

---

## Changelog

- **2026-08-06** — кнопка переименована в «Обновить Google» и больше не создаёт
  отдельный файл: пишет ту же вкладку «..._Автоматическая», что ночная выгрузка
  (создание файла отвечало 403 — у сервис-аккаунта нет своего Диска).
- **2026-08-01** — руководство создано вместе с интеграцией
  (`core/salary_gsheet.py`, кнопка «Экспорт Google» на `/salary`).
