# 🗄️ Настройка Render Disk для хранения данных кранов

## ✅ Что сделано:

1. **Создан Render Disk** с mount path `/kultura`
2. **Обновлён код** для автоматического использования диска
3. **Данные теперь постоянны** - переживают любые редеплои

---

## 📋 Инструкция по миграции данных на диск

### Шаг 1: Деплой обновлённого кода

```bash
# На вашем компьютере уже сделано:
git add app.py
git commit -m "Интеграция с Render Disk /kultura"
git push
```

После push Render автоматически сделает редеплой.

---

### Шаг 2: Копирование данных на диск (ВАЖНО!)

После деплоя зайдите в **Render Dashboard → Shell** и выполните:

```bash
# 1. Проверьте что диск примонтирован
ls -la /kultura

# 2. Скопируйте текущие данные на постоянный диск
cp data/taps_data.json /kultura/taps_data.json

# 3. Проверьте что файл скопировался
ls -lh /kultura/taps_data.json
cat /kultura/taps_data.json | head -20

# 4. Перезапустите приложение (или Render сделает это автоматически)
# В логах вы должны увидеть:
# [INFO] Используется Render Disk: /kultura/taps_data.json
```

---

### Шаг 3: Проверка работы

1. **Откройте страницу кранов** на вашем сайте
2. **Измените любой кран** (например, подключите кегу)
3. **В Render Shell проверьте**:

```bash
# Проверьте что изменения сохранились на диске
ls -lh /kultura/taps_data.json
# Размер файла должен измениться

# Посмотрите последние изменения
tail -50 /kultura/taps_data.json
```

4. **Сделайте редеплой** (измените что-то в коде и сделайте git push)
5. **После редеплоя проверьте** что данные кранов остались на месте

---

## 🎉 Преимущества Render Disk:

- ✅ **Постоянное хранение** - данные переживают редеплои
- ✅ **Автоматические бэкапы** от Render (зависит от плана)
- ✅ **Быстрый доступ** - диск примонтирован как обычная папка
- ✅ **Независимость от git** - не нужно коммитить данные

---

## 📊 Как это работает:

```
Приложение запускается
         ↓
Проверяет: существует /kultura?
         ↓
    Да → Использует /kultura/taps_data.json (постоянный диск)
    Нет → Использует data/taps_data.json (локально для разработки)
         ↓
Все изменения кранов сохраняются на постоянный диск
         ↓
При редеплое Render:
  - Создаётся новый контейнер
  - Диск /kultura остаётся примонтированным
  - Данные НЕ теряются! ✅
```

---

## 🔄 Резервное копирование:

### Автоматическое (если настроено в Render):
Render может делать автоматические snapshot диска.

### Ручное:
```bash
# В Render Shell
# Создать бэкап
cp /kultura/taps_data.json /kultura/taps_data_backup_$(date +%Y%m%d).json

# Посмотреть все бэкапы
ls -lh /kultura/taps_data_backup_*.json

# Восстановить из бэкапа
cp /kultura/taps_data_backup_20251102.json /kultura/taps_data.json
```

### Скачать на компьютер:
```bash
# В Render Shell
cat /kultura/taps_data.json

# Скопировать вывод и сохранить локально
```

---

## 🆘 Устранение проблем:

### Проблема: Приложение не видит диск

**Проверка:**
```bash
# В Render Shell
ls /kultura
echo $?  # Должно быть 0
```

**Решение:**
- Проверьте что диск подключен к сервису в Render Dashboard
- Перезапустите сервис

### Проблема: Данные не сохраняются

**Проверка:**
```bash
# Проверьте права на запись
touch /kultura/test.txt
ls -la /kultura/test.txt
rm /kultura/test.txt
```

**Решение:**
- Проверьте права доступа к диску
- Перезапустите сервис

### Проблема: Приложение использует старый путь

**Проверка логов:**
В логах Render должна быть строка:
```
[INFO] Используется Render Disk: /kultura/taps_data.json
```

Если видите:
```
[INFO] Используется локальный путь: data/taps_data.json
```

**Решение:**
- Убедитесь что диск примонтирован: `ls /kultura`
- Перезапустите сервис

---

## ✅ Чек-лист настройки:

- [ ] Создан Render Disk с mount path `/kultura`
- [ ] Диск подключен к сервису
- [ ] Обновлённый код задеплоен (app.py)
- [ ] Данные скопированы: `cp data/taps_data.json /kultura/taps_data.json`
- [ ] В логах видно: `[INFO] Используется Render Disk: /kultura/taps_data.json`
- [ ] Изменения кранов сохраняются
- [ ] После редеплоя данные остались на месте

---

**Дата создания:** 2 ноября 2025
**Mount Path:** `/kultura`
**Файл:** `/kultura/taps_data.json`
