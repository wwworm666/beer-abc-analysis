# График смен

## Что это

Управление графиком смен сети из 4 баров. Две страницы с разным назначением:

- **`/schedule` — просмотр** (для барменов): сетка «дни x бары», кто где работает,
  ввод факта отработанных часов. Финансовых данных на странице нет.
- **`/schedule/edit` — рабочий стол владельца**: кисть для назначения смен с
  пресетами времени, предупреждения (незакрытые точки, конфликты с выходными),
  план/факт выручки по дням, нагрузка по сотрудникам, сводка месяца, пожелания,
  реестр сотрудников.

Эталоном функционала была гугл-таблица владельца (дни x сотрудники, часы, итоги);
веб-версия хранит те же данные в SQLite и считает агрегаты сама.

## Файлы

| Файл | Роль |
|------|------|
| `routes/pages.py` | роуты страниц `/schedule`, `/schedule/edit` |
| `routes/schedule.py` | все API `/api/schedule/*` + meeting-notes |
| `core/shifts_manager.py` | SQLite-менеджер (`data/shifts.db`, на проде `/kultura/shifts.db`) |
| `core/schedule_plans.py` | чистые функции: план дня с фоллбэком, сводка месяца, нагрузка |
| `core/calendar_ics.py` | генератор iCalendar (.ics) личного графика — экспорт в календарь телефона |
| `core/meeting_notes.py` | заметки совещаний (без изменений) |
| `templates/schedule.html` | страница просмотра |
| `templates/schedule_edit.html` | страница редактора |
| `static/schedule/schedule.css` | общие стили обеих страниц |
| `static/js/schedule/common.js` | API-клиент, состояние, рендер сетки |
| `static/js/schedule/view.js` | логика просмотра + модалка факта часов |
| `static/js/schedule/edit.js` | кисть, предупреждения, панель дня, сводка, реестр, история |
| `tests/test_schedule_v3.py` | миграция схемы, факт часов, реестр, формулы |
| `tests/test_schedule_audit.py` | журнал изменений: схема v4, log/get_audit, get_shift |
| `tests/test_calendar_ics.py` | генератор .ics: структура, день/вечер, факт/план, экранирование |

## Как работает

### Схема БД (v5)

SQLite `shifts.db`, WAL + busy_timeout (gunicorn 2 workers). Миграции **только
additive** (`PRAGMA table_info` -> `ALTER TABLE ADD COLUMN`, `CREATE TABLE IF NOT
EXISTS`); перед миграцией менеджер снимает консистентный бэкап через SQLite
backup API в `shifts.db.backup_v{старая_версия}`. DROP таблиц запрещён — в проде
живые данные.

- `locations` — 4 бара; `venue_key` — мост к планам
  (`varshavskaya`, `bolshoy`, `kremenchugskaya`, `ligovskiy`).
- `roles` — бармен / второй бармен (роль «стажёр» убрана 2026-06-24, владелец:
  только две роли); `color` — цвет **точки роли на чипе** смены (не заливка, см.
  «Внешний вид сетки»); `rate_per_hour` (v5) — ставка за час для ЗП (DEFAULT 300).
- `shifts` — смена: `date`, `employee_id` (стабильный id из iiko, v6 — ключ
  сотрудника), `employee_name` (снимок имени для показа/фоллбэка, канонизируется
  при синке), `location_id`, `role_id`, `start_time`, `fact_minutes`.
- `day_off_requests`, `wishes`, `daily_revenue` — как раньше (+ `employee_id`
  впрок, v6).
- `schedule_employees` — реестр: `name` (PK, текущее имя из iiko), `iiko_id`
  (v6, стабильный ключ — UNIQUE), `short_label` («РЮ»), `active`, `sort_order`.
  Идентичность — по `iiko_id`; см. «Идентичность сотрудника».
- `schedule_audit` (v4) — журнал изменений графика (append-only): `ts`,
  `actor_login`, `actor_name`, `action`, `entity_date`, `employee_name`,
  `summary`. См. раздел «Журнал изменений».

### Идентичность сотрудника — стабильный id из iiko (v6)

**Зачем.** Раньше сотрудник в смене и реестре хранился строкой-именем. iiko-синк
делал `INSERT OR IGNORE` по имени и ничего не удалял, поэтому переименование
сотрудника в iiko плодило дубли: новое имя приходило новой записью, а прошлые
смены оставались под старым. Ручная чистка не масштабируется.

**Как стало.** Сотрудник идентифицируется стабильным `id` из справочника iiko
(`/employees`, GUID; те же id, что в `responsibleUserId` кассовых смен). Смена
хранит `employee_id`; `employee_name` — только снимок для показа/фоллбэка.
Реестр (`schedule_employees`) ключуется `iiko_id`. Имя везде резолвится из
реестра по id (фронт: `Schedule.shiftDisplayName` / `shiftLabel`).

**Синк** (`ShiftsManager.sync_employees(pairs, overrides)`, эндпоинт
`POST /api/schedule/employees/sync`) за одну транзакцию, идемпотентно:
1. распространяет переименования: `shifts.employee_name` по `employee_id`;
2. бэкофиллит `employee_id` у смен без него: override → точное имя → однозначная
   перестановка слов (`«Васильев Никита» == «Никита Васильев»`);
3. upsert реестра по `iiko_id` (имя обновляется; legacy-строка с тем же именем
   «усыновляется» — получает id, сохраняя метку/порядок/активность); новые люди
   из справочника **без смен** добавляются неактивными (скрыты из кисти, видны в
   админке — владелец включает барменов), со сменами — активными;
4. удаляет осиротевшие legacy-строки реестра.

`overrides` (`{старое_имя_смены: iiko_id_или_текущее_имя}`) — разовая ручная
привязка наследия, где имя изменилось так, что авто-сопоставление по строке
невозможно (напр. «Артемий»→«Артем»). После бэкофилла переименования
подхватываются автоматически — **дубли больше не возникают**. Тесты:
`tests/test_employee_ids.py`.

### Два слоя времени смены

- **План**: `start_time` `'HH:MM'`. Смены двух типов: **день** (с 14:00,
  норма — хранится как `NULL`, бейдж времени на чипе не показывается, роль
  по умолчанию бармен) и **вечер** (`'18:00'`, бейдж «с 18:00», роль по
  умолчанию второй бармен; меняется в модалке). Другое время — только
  вручную через модалку для нестандартных случаев.
- **Факт**: `fact_minutes` — вводится **барменом руками в конце смены**
  (страница просмотра, клик по своей смене). Смена может длиться дольше
  кассовой, поэтому по API iiko факт часов вытащить нельзя — ручной ввод
  является единственным источником факта часов для зарплатных расчётов.
  Прошедшие смены без факта попадают в «Без часов».
### Виджет рейла «Нагрузка» (редактор И просмотр, money-free)

«Нагрузка» общий для `/schedule/edit` и `/schedule` — рендер один
(`Schedule.renderLoad` в `common.js`), данные — **money-free** эндпоинт
`GET /api/schedule/widgets/<y>/<m>` (без iiko, без рублей). На просмотре read-only —
у барменов финансов на странице нет.

**Нагрузка по сотрудникам** (группировка по стабильному `employee_id`):
- **Смен / 15** — число смен против нормы `SHIFT_NORM=15` (`core/schedule_plans.py`,
  то же значение, что в KPI-коэффициенте). Цвет: ≥нормы — зелёный, <60% нормы —
  янтарный (недобор), иначе обычный.
- **Часов** — сумма факта часов (ручной ввод бармена; единственный источник часов оплаты).
- **Подряд** — макс. серия смен подряд по календарным дням (`_max_consecutive_days`);
  5+ подряд — красный флаг переработки.
- **Без** — прошедшие смены без введённого факта.

### Виджет «План / Факт по дням» (деньги, обе страницы)

**Одна таблица по всем точкам**, без вкладок: строка — день, колонки — каждая ТТ
(факт сверху, план снизу; цвет факта по уровню), плюс «Итого» (день) и **% бейдж**.
Внизу строка «Итого» — суммы по каждой ТТ за месяц и общий итог. Факт — живой iiko
OLAP (тот же источник, что «Факт» на дашборде).

- **Свёрнута по умолчанию**: заголовок-тоггл (`#planFactToggle` → `#planFactCollapse`);
  клик раскрывает/сворачивает. В заголовке (видна и в свёрнутом виде) — месячная
  сводка `.pf-summary`: `Σфакт / Σплан · %`.
- **Деньги в ячейках сокращены** (`formatK`: «134к», «2.7М») ради плотности; точные
  рубли — в `title` ячейки (hover).
- **Клик по строке дня** раскрывает «кто работал» на каждой ТТ (чипы по стабильному
  `employee_id`) — `pfPeople`. Деньги/% уже в самой строке, поэтому разворот только
  про людей.
- **Общий рендер** `Schedule.renderPlanFact(host)` (`common.js`) — на `/schedule/edit`
  И `/schedule`. Доступ к обеим страницам одинаковый (равные права аккаунтов), поэтому
  отдельной «money-free» страницы для барменов больше нет — владелец решил видеть
  выручку и в просмотре.
- **Без отдельного эндпоинта и без второго вызова iiko**: рендерится на фронте из
  уже загруженного `S.state.plans` (`GET /api/schedule/plans` →
  `build_month_plans(..., olap_fact=...)`) и `S.state.shifts`. View подтягивает `/plans`
  в `loadPlanFact`.
- **% бейдж**: ≥100 зелёный, 85..99 янтарный, <85 красный; нет факта — «—».
- **Итоговый %** = `Σфакт / Σплан тех дней, где факт уже есть` (как completion на
  дашборде): иначе будущие дни с планом без факта занижали бы выполнение.

> Примечание: `/api/schedule/widgets` («Нагрузка») остаётся money-free — рублей в нём
> нет; деньги приходят отдельным `/plans`.

Финансовой «Сводки месяца» на странице графика нет — агрегаты живут на дашборде
(убрана 2026-06-25). Старый роут `/api/schedule/summary` фронтом не используется.

### Мини-виджет «Выполнение плана» (точки × 3 дня, рейл под «Нагрузкой»)

`Schedule.renderRecentCompletion(host)` (`common.js`) — на обеих страницах, в правом
рейле под «Нагрузкой». Компактная **матрица**: строки — точки (+ строка «Все» снизу,
агрегат), колонки — **позавчера / вчера / сегодня**. Ячейка — % выполнения за день по
точке (`факт / план` из `d.locations[id]`), цвет по уровню (зелёный/янтарный/красный).
Колонка «сегодня» — последняя, выделена акцентом (факт неполный, день не закрыт; см.
подпись «сегодня — идёт»). Даты реальные (`new Date()`), данные из `state.plans`
отображаемого месяца; дата вне месяца → «—» (виджет рассчитан на текущий месяц).

### Чип смены (сетка, обе страницы)

`buildCellChip` (`common.js`) рендерит все смены ячейки **одной карточкой** (без рамки,
поверхность — сама ячейка): бармены в строку через «/», у каждого — **цветная точка
роли** (`--role-color`: зелёная — день/бармен, синяя — вечер/второй бармен). Одиночная
смена — **имя целиком** тянется на всю ширину ячейки, справа ровным столбцом — мета.
Двойная — фамилии без меты (полное имя/время/факт в подсказке).

**Мета зависит от состояния смены** (всё из данных, без новых запросов): факт проставлен
→ **отработанные часы**; сегодня без факта → **«идёт»**; прошлая смена без факта →
**«факт?»** (янтарный флаг, надо дозакрыть); будущее → **«с HH:MM»** (плановое начало).
Так визуально разделены отработанные и неотработанные смены.

### Пожелания на странице просмотра (read-only)

Пожелания сотрудников видны и на `/schedule` (read-only, `Schedule.renderWishesReadonly`),
не только в редакторе — бармены видят, что просили учесть. Правка пожеланий —
по-прежнему только в редакторе (textarea с автосейвом).

### Внешний вид сетки (визуальная дисциплина)

- **Роль = цветная точка** (`--role-color`, прокидывается в `common.js: buildCellChip`),
  НЕ заливка чипа — иначе сетка превращается в кислотный heatmap на тёплой палитре бренда.
- **Состояние смены = яркость/метка**: отработанное прошлое приглушено (`st-done`),
  прошлое без факта — янтарный «факт?» (`st-nofact`), сегодня — «идёт» (`st-today`),
  будущее — полный цвет. Сетка делится по линии «сегодня» на «было» и «будет».
- **Рамка недели Пн→Вс**: «канавка» между неделями (`.weekgap`) + линии band-а
  (`.wk-top`/`.wk-bottom`, цвет `--text-tertiary`); внутренние линии дней мягче
  (`--border-light`), чтобы рамка выделялась.
- **Каналы независимы, не путать**: фон ячейки = статус (дыра/выходной/сегодня/конфликт),
  точка = роль, яркость = отработана/нет.
- **Выходные = пятница и суббота** (`dow 5/6`): тёплый фон + акцентное число; воскресенье
  выходным не считаем (бар: Пт-Сб — пик).
- **Радиусы и тени — из дизайн-системы** (`variables.css`): не заводить локальные «свои»
  радиусы — страница должна быть неотличима по языку от дашборда.

### Раскладка редактора

- **Липкая панель управления** (`.sched-controls`): месяц-нав + кисть +
  предупреждения едут за прокруткой (`position: sticky`, z 50 — ниже модалки), чтобы
  кисть оставалась под рукой на длинной сетке.
- **Две колонки** (`.sched-layout`) на экране > 1180px: сетка слева, справочный рейл
  (Нагрузка + Сводка) справа — read-only блоки не растягивают вертикальный скролл.
  Уже — одна колонка, рейл уезжает под сетку. Пожелания / Сотрудники / История —
  на всю ширину под раскладкой.

### План выручки дня — read-only

Ручной ввод плана на странице графика **заморожен** (2026-06). Источник —
конвейер планов: `plansdashboard.json` -> `core/day_weights.py`
(пт/сб вес 2.0, override праздников) -> `data/daily_plans.json`.

Правило отображения плана точки на дату (`core/schedule_plans.py`):

1. есть `venue_key` в `daily_plans` за дату и значение > 0 -> план «из весов»;
2. иначе есть старый ручной `daily_revenue.plan_revenue` > 0 -> «ручной (устар.)»;
3. иначе — **«нет плана»** (None, никогда не 0: отсутствие плана не равно нулю).

Менять план — через «Планы по дням» в дашборде (override веса), не здесь.

### Факт выручки — автоматически из iiko OLAP (как на дашборде)

Факт подтягивается живьём при загрузке `plans`/`summary` — тем же способом
и метрикой, что «Факт» на дашборде:

- OLAP `SALES`, группировка `Store.Name + OpenDate.Typed`, агрегат
  `DishDiscountSumInt` (`core/olap_reports.py: get_store_daily_revenue`);
- общий кэш дашборда `cached_olap` (`extensions.py`): TTL 10 минут +
  single-flight, ключ `schedule_fact_{YYYY-MM}`;
- маппинг `Store.Name -> venue_key` через `venues_manager`
  (имена точек в iiko совпадают с нашими).

Если iiko недоступен — фоллбэк на сохранённый `daily_revenue.fact_revenue`
(наполняется legacy-эндпоинтами `POST /api/schedule/revenue/sync[-month]`,
из UI они больше не вызываются). Отсутствие продаж за день = «нет данных»,
не ноль.

### Формулы сводки месяца (тултипы в UI показывают эти же тексты)

| Метрика | Формула |
|---------|---------|
| План месяца | сумма планов всех дней, где план есть |
| Факт | сумма синкнутого факта |
| Средняя в день | факт / число дней с фактом |
| Ожидаемая | сумма по дням: факт, если есть, иначе план (веса пт/сб учтены в плане) |
| Выполнение % | факт / план тех же дней, где факт уже есть, x 100 |

«Ожидаемая» здесь — сознательно ДРУГАЯ метрика, чем `expected` дашборда
(`core/revenue_metrics.py` — линейная экстраполяция среднего): формула графика
weight-aware — повышенные планы пт/сб и праздничные override в остатке месяца
учитываются автоматически.

### Предупреждения (детерминированные, считаются на клиенте)

- **Не закрыто**: будущая дата (включая сегодня), точка без единой смены.
  Прошедшие пустые дни нейтральны — дырой не считаются.
- **Конфликт с выходным**: смена в день, попадающий в интервал day-off заявки
  того же сотрудника. Назначить можно (подтверждение), маркер остаётся.

### Журнал изменений (кто что менял)

Появился вместе с авторизацией: раньше график правили анонимно, теперь у каждого
действия есть автор (текущий пользователь сессии, `core/auth_guard: current_user`).

- **Что фиксируется**: создание / изменение / удаление смены, простановка и
  очистка факта часов, добавление / удаление пожелания выходного. По одной
  append-only записи на действие в `schedule_audit`. Выручка, пожелания (текст) и
  правки реестра пока не журналируются (выручка тянется из iiko автоматически,
  остальное — не «изменения графика» в смысле задачи).
- **Где пишется**: на уровне роутов (`routes/schedule.py`, хелпер `_audit`), а не
  менеджера — там доступен `current_user`, а сам `ShiftsManager` остаётся чистым
  хранилищем без зависимости от веб-слоя. Для удаления и изменения снимок «до»
  берётся через `get_shift()` / `get_day_off_request()` **перед** мутацией, чтобы
  в записи было читаемое описание (кого/где/когда).
- **`actor_name` — снимок** `display_name` автора на момент действия: журнал
  читается, даже если аккаунт потом переименуют или удалят (не FK на `users`).
- **Best-effort**: `_audit` обёрнут в try/except — сбой журналирования никогда не
  валит саму операцию со сменой (логируется предупреждением в stderr).
- **`entity_date`** — дата, к которой относится изменение; по ней история
  фильтруется на месяц, который открыт в редакторе.
- **Где видно**: две точки показа одной и той же ленты (`GET /api/schedule/audit`),
  обе за открытый месяц, новые сверху:
  - `/schedule/edit` — панель «История изменений» (сворачиваемая, ленивая,
    обновляется на каждый reload), формат «ДД.ММ ЧЧ:ММ — Автор — что сделал».
  - `/schedule` (просмотр, для барменов) — компактная лента «Последние изменения»
    под сеткой: цветная точка по типу действия (создание/удаление/факт/выходной),
    `limit=8`. Факт часов бармены вводят здесь же, и запись пишет тот же серверный
    роут — поэтому видно, кто проставил себе часы.

`ts` пишется в локальном серверном времени (как `created_at`/`updated_at`),
фронт показывает его как есть, без пересчёта таймзоны.

### Оплата часов по ролям (источник часов для ЗП)

С 2026-06-24 **график — единственный источник часов оплаты** (раньше страница ЗП
брала часы из кассовых смен iiko). Причина: на смену могут выходить двое — один
на полную (день, с 14:00), другой с вечера (18:00), и ставка за час у них разная;
а смена может длиться дольше кассовой, поэтому iiko-часы для оплаты не подходят.

Модель: **ставка — у роли** (`roles.rate_per_hour`), **часы — факт по каждой
смене** (`shifts.fact_minutes`). Оплата сотрудника за период =
`Σ по сменам: часы смены × ставку её роли`. Полная смена = роль «бармен»,
вечерняя = «второй бармен» (другая ставка). Ролей две (стажёр убран). До правки
владельцем ставка у всех ролей = 300 (старая единая), поэтому миграция оплату
не меняет.

- **Расчёт** — `ShiftsManager.get_hours_by_role_for_period(date_from, date_to)`:
  группирует смены периода по сотруднику и роли, суммирует `fact_minutes`
  (NULL не суммируется), считает `pay = часы × rate_per_hour`. Возвращает разбивку
  по ролям + `total_pay`, `total_hours`, `shifts_without_fact` (смены без факта —
  чтобы на ЗП было видно пробелы). Детерминированно, тестируется
  (`tests/test_salary_hours.py`).
- **API** — `GET /api/schedule/hours-by-role?date_from=&date_to=` (даты
  включительно) -> `{rates: роли со ставками, employees: [...]}`. Страница ЗП
  (`templates/bonus.html`) вызывает его рядом с `bonus`/`kpi` и подмешивает часы
  по имени сотрудника. Часы на ЗП **read-only** (правка — в графике, изменения
  пишутся в журнал); ставки правятся там же блоком «Ставки за час по ролям»
  (`PUT /api/schedule/role/<id>/rate`).
- **Ставка не снапшотится в смену**: меняешь ставку роли — пересчитываются все
  смены периода по новой ставке (ЗП считается помесячно, ставки меняются редко).
- iiko-часы (`total_hours`) остаются метрикой Employee Dashboard (выручка/час),
  но в оплату ЗП больше не входят. См. `docs/employee.md`, «Расчёт часов работы».

### Экспорт личного графика в календарь (.ics)

Бармен скачивает СВОЙ график одним файлом и открывает в календаре телефона —
iOS (Apple Calendar) и Android (Google Calendar) понимают **iCalendar (.ics)**
нативно. Разовая выгрузка (не подписка): поменялся график — скачать заново.

- **Кнопка** «В календарь» на `/schedule` (в шапке месяца) — видна, только если
  аккаунт привязан к сотруднику (`users.employee_iiko_id`); иначе экспортировать
  нечего.
- **Маршрут** `GET /schedule/cal.ics` (за логином, не публичный): отдаёт смены
  ТЕКУЩЕГО пользователя (`current_user().employee_iiko_id` ->
  `shifts.employee_id`, `ShiftsManager.get_shifts_for_employee`). Окно — месяц
  назад .. полгода вперёд. `Content-Type: text/calendar`, как вложение.
- **Генерация** — `core/calendar_ics.build_calendar` (чистая, детерминированная,
  кроме `DTSTAMP`; тест `tests/test_calendar_ics.py`):
  - одно событие на смену; `UID = shift-<id>@...` стабилен -> повторный импорт
    ОБНОВЛЯЕТ событие, а не плодит дубли;
  - время плавающее локальное (без TZID/Z — вся сеть в Москве);
  - начало: `start_time` (вечер 18:00) либо 14:00 («день»); длительность: факт
    часов, иначе план-дефолт по роли (день 9ч / вечер 5ч);
  - спецсимволы экранируются (RFC 5545), строки через CRLF, без свёртки (чтобы не
    рвать UTF-8 кириллицу посреди символа).
- Подписки (авто-обновление) сознательно нет — выбран разовый .ics: проще и без
  публичного эндпоинта/токена.

### API

| Метод | Путь | Что делает |
|-------|------|-----------|
| GET | `/api/schedule/employees[?all=1]` | реестр (+имена из истории смен) |
| POST | `/api/schedule/employees/sync` | синк реестра с iiko по id (бэкофилл/дедуп/переименования); body опц. `{overrides}` |
| PUT | `/api/schedule/employee/<iiko_id>` | short_label / active / sort_order (по id) |
| GET | `/api/schedule/roles`, `/api/schedule/locations` | справочники (roles c rate_per_hour) |
| PUT | `/api/schedule/role/<id>/rate` | ставка за час роли (для ЗП) |
| GET | `/api/schedule/hours-by-role?date_from=&date_to=` | часы по ролям + оплата за период (страница ЗП) |
| GET | `/api/schedule/<year>/<month>` | смены месяца |
| POST/PUT/DELETE | `/api/schedule/shift[/<id>]` | CRUD смены (+`start_time` HH:MM) |
| PUT | `/api/schedule/shift/<id>/fact` | факт минут (0..1440 или null) |
| GET/POST/DELETE | `/api/schedule/dayoff` | заявки на выходные |
| GET | `/api/schedule/audit/<year>/<month>[?limit=]` | журнал изменений за месяц (новые сверху) |
| GET | `/schedule/cal.ics` | личный график текущего пользователя в .ics (за логином, для календаря телефона) |
| GET | `/api/schedule/plans/<year>/<month>` | план/факт по дням (план read-only) |
| GET | `/api/schedule/widgets/<year>/<month>` | данные виджета «Нагрузка», money-free, без iiko — для редактора и просмотра |
| GET | `/api/schedule/summary/<year>/<month>` | сводка месяца + нагрузка (legacy, фронтом не используется) |
| GET | `/api/schedule/revenue/<date>` | план/факт дня по точкам |
| PUT | `/api/schedule/revenue/<date>/<loc>` | факт дня (`plan_revenue` игнорируется) |
| POST | `/api/schedule/revenue/sync/<date>` | legacy: наполнить фоллбэк-кэш факта |
| POST | `/api/schedule/revenue/sync-month/<y>/<m>` | legacy: то же за месяц |
| GET/POST | `/api/meeting-notes[...]` | заметки совещаний (без изменений) |

## Changelog

### 2026-06-27 (5) — экраны «kultura.os»: полосы по людям, сегодня вживую, мобильный
- **Новая визуальная ДНК** (`static/js/schedule/screens.js` + блок CSS в schedule.css):
  цвет блока = точка (Варш голубой / ВО розовый / Крем жёлтый / Лиг зелёный),
  высота блока = смена (полный — день, низкий — вечер), рамка = статус (терракот —
  сегодня, янтарь — без факта, красный — конфликт). Источник — Claude Design
  «График смен — идеи». Это снимает прежний упрёк «цвет почти всегда зелёный».
- **Страница `/schedule` пересобрана** на три экрана — всё из уже загруженного state,
  без новых запросов:
  - **Полосы по людям** (`renderLanes`): люди-строки × дни-колонки, блоки-смены,
    gauge «смен/15» (цвет по выполнению), флаг переработки (6+ подряд);
  - **Сегодня вживую** (`renderTodayBoard`): 4 точки сейчас (день+вечер, план %),
    очередь действий (дыра / конфликт / нет факта);
  - **Мобильный «мои смены»** (`renderMyShifts`, ≤700px): сегодня-идёт, «нужен факт
    часов», моя полоса, ближайшие смены, выходной/.ics. Решает мобильную боль
    (таблица «4 бара в ширину» не влезала в телефон → отдельный экран бармена).
- Старая сетка дата×точка и рейл-виджеты («Нагрузка», «Выполнение плана») с `/schedule`
  убраны (заменены экранами). Десктоп-виджеты владельца (План/Факт, Пожелания, Лента)
  скрыты на мобильном (`.sc-desktop`/`.sc-mobile`).
- **Редактор `/schedule/edit` не тронут**: назначение смен (кисть) и старая сетка
  работают как раньше; данные смен и факта часов целы (менялась только презентация).
- Ввод факта сохранён: клик по блоку (полосы) или карточке «нужен факт» (мобильный)
  открывает модалку через `Schedule.setScreenShiftClick`.

### 2026-06-26 (4) — ячейка сетки: имя на всю ширину, состояния смены, рамка недели
- **Ячейка пересобрана** (`buildCellChip` + `schedule.css`): убрана рамка-в-рамке (чип
  без бордюра, поверхность — сама ячейка); имя одиночной смены тянется на всю ширину
  (снят `max-width: 14ch`), мета (факт/начало) прижата вправо ровным столбцом (tabular-nums).
- **Разделение отработанных и неотработанных смен** (из данных, без новых запросов):
  отработанное прошлое приглушено (`st-done`), прошлое без факта — янтарный «факт?»
  (`st-nofact`), сегодня — «идёт» (`st-today`), будущее — полный цвет. Мета стала
  состояние-зависимой (часы / идёт / факт? / с HH:MM).
- **Рамка недели Пн→Вс**: «канавка» (`.weekgap`) + линии band-а (`.wk-top`/`.wk-bottom`);
  внутренние линии дней мягче (`--border-light`).
- Выходные Пт/Сб визуально усилены (тёплый фон, акцентное число); логика `dow 5/6` без
  изменений. Запись закрывает и недокументированный переход чипа на точки роли (6d92d07).

### 2026-06-26 (3) — экспорт личного графика в календарь (.ics)
- Новый модуль `core/calendar_ics.py` (`build_calendar`) — генерация iCalendar
  личного графика; чистая, детерминированная, тест `tests/test_calendar_ics.py` (5).
- Маршрут `GET /schedule/cal.ics` (за логином): смены `current_user` по
  `employee_iiko_id`, окно месяц назад .. полгода вперёд; метод
  `ShiftsManager.get_shifts_for_employee`. Кнопка «В календарь» на `/schedule`
  (видна при привязке аккаунта к сотруднику).
- Разовая выгрузка (по решению владельца), не подписка: проще, без публичного
  эндпоинта/токена. iOS (Apple Calendar) и Android (Google Calendar) импортируют
  .ics нативно. См. раздел «Экспорт личного графика в календарь (.ics)».
- Импорт исторического графика из старой Google-таблицы (фев–июнь 2026, ~616 смен)
  выполнен отдельно (данные, не код); короткие смены второго бармена помечены
  «вечер» (start_time 18:00).

### 2026-06-26 (2) — «План / Факт по дням» вместо «Покрытия» + чип сетки
- Виджет «Покрытие по дням недели» убран (владелец: «не то» — нужны деньги, а не
  относительный спрос). Удалены `compute_coverage_by_dow`, `Schedule.renderCoverage`,
  CSS `.cov-*`, тест `test_coverage_by_dow`; `/widgets` отдаёт только «Нагрузку».
- Новый денежный виджет **«План / Факт по дням»** (`renderPlanFact` в `common.js`,
  общий): за каждый день — план/факт выручки, % выполнения, кто на смене; клик по
  дню — разбивка по барам. Строится на фронте из уже загруженного `/plans` (факт =
  живой iiko OLAP) и `S.state.shifts` — **без нового эндпоинта и без второго вызова iiko**.
- Итоговый % — факт к плану только дней с фактом (как completion на дашборде).
- **На обеих страницах**: владелец попросил видеть выручку и в просмотре `/schedule`
  (view тянет `/plans` в `loadPlanFact`). Доступ к `/schedule` и `/schedule/edit`
  одинаковый (равные права) — отдельной money-free-страницы больше нет. «Нагрузка»
  (`/widgets`) остаётся money-free.
- **Чип смены переработан** (`buildChip`): имя целиком + строка-мета (капсула
  «День»/«с HH:MM» + факт часов) вместо двухбуквенной метки — ячейка перестала
  выглядеть пустой. Роль по-прежнему = цвет левой полоски.
- Фикс XSS: `loc.name` в `renderDayPanel` теперь экранируется (нашёл адверсариал-ревью;
  баг был пред-существующий, но новый код ту же локацию уже экранировал).

### 2026-06-26 — виджет «Покрытие по дням недели» + виджеты и пожелания на просмотре
- Новый money-free виджет «Покрытие по дням недели» (`compute_coverage_by_dow`):
  относительный спрос (план дня как доля, без рублей) vs смены/день; подсветка
  самого недозакрытого под спрос дня. Эндпоинт `GET /api/schedule/widgets` (без
  iiko, без денег) — общий для редактора и просмотра.
- «Нагрузка» и «Покрытие» вынесены в общие рендеры (`common.js`) и **продублированы
  на страницу просмотра** `/schedule` (read-only). «Пожелания» тоже видны на
  просмотре (read-only). Идея виджета — из deep-research по ростер-дашбордам
  (coverage vs demand), адаптирована money-free под страницу барменов.
- Тест `tests/test_employee_ids.py::test_coverage_by_dow`.
- _Заменён в тот же день (см. запись выше): «Покрытие» убрано в пользу «План/Факт по дням»._

### 2026-06-25 — «Нагрузка» расширена, «Сводка месяца» убрана
- Убрана финансовая «Сводка месяца» (план/факт/выполнение) — выручка не нужна на
  странице построения графика. Заодно убран вызов iiko-часов из summary-роута
  (колонки давно нет → лишняя задержка).
- «Нагрузка по сотрудникам» расширена: **Смен / 15** (норма `SHIFT_NORM`, цвет по
  выполнению), **Подряд** (макс. серия смен подряд, флаг переработки 5+), плюс
  прежние Часов и Без. Группировка по стабильному `employee_id`. См. раздел
  «Нагрузка по сотрудникам». Тест `tests/test_employee_ids.py::test_load_streak_and_grouping`.

### 2026-06-24 — сотрудники по стабильному id из iiko (схема v6)
- Корень проблемы дублей: сотрудник хранился строкой-именем, синк по имени не
  удалял старое → переименование в iiko плодило дубли. Решение: `shifts.employee_id`
  + `schedule_employees.iiko_id` (GUID из iiko), имя — только показ/фоллбэк.
- `sync_employees(pairs, overrides)` (эндпоинт через `iiko.get_employees()`):
  бэкофилл id у смен (override → точное → перестановка слов), канонизация имён,
  распространение переименований по id, дедуп legacy-строк, новые без смен —
  неактивны. Идемпотентно. Переименование в iiko больше не плодит дублей.
- Фронт: идентичность по id (`shiftDisplayName`/`shiftLabel`, кисть и реестр по
  id, `PUT /employee/<id>`). Часы-по-ролям группируются по id (rename-proof).
- Тесты: `tests/test_employee_ids.py` (7). Раздел «Идентичность сотрудника».

### 2026-06-24 — визуальная причёска сетки (см. «Внешний вид сетки» / «Раскладка»)
- Чип смены: из насыщенной заливки роли в спокойную карточку с цветной полоской
  слева (`--role-color`); сетка перестала конфликтовать с тёплой палитрой бренда.
- Радиусы/тени `schedule.css` приведены к токенам `variables.css` (16/10px,
  pill-кнопки, `--shadow-sm` на карточках) — убрана «своя» дизайн-система страницы.
- Колонка «Часов (iiko)» убрана из «Нагрузки»; лента на `/schedule` — те же токены.
- Раскладка редактора: липкая панель управления (`.sched-controls`) + две колонки
  (`.sched-layout`, сетка слева, рейл Нагрузка+Сводка справа на > 1180px).
- Только CSS + разметка-обёртки + `buildChip` (цвет роли в CSS-переменную), логику
  не трогали.

### 2026-06-24 — оплата часов по ролям из графика (схема v5)
- `roles.rate_per_hour` (ставка за час, DEFAULT 300) + `set_role_rate` +
  `get_hours_by_role_for_period` в `ShiftsManager`. Миграция v4 -> v5 additive.
- **График — единственный источник часов оплаты** (iiko-часы для ЗП убраны):
  оплата = Σ по сменам (`fact_minutes` × ставка роли). На смену могут выходить
  двое (полная + вечер с 18:00) с разной ставкой — это и решает модель «ставка
  у роли».
- `GET /api/schedule/hours-by-role`, `PUT /api/schedule/role/<id>/rate`. Страница
  ЗП (`bonus.html`): часы read-only по ролям из графика, убран хардкод
  `HOURLY_RATE=300` и ручной ввод часов, добавлен блок «Ставки за час по ролям».
- Роль **«стажёр» убрана** (владелец: только бармен + второй бармен). Удаление
  идемпотентное и безопасное — только если на роль не ссылается ни одна смена
  (иначе осиротели бы); из сид-данных тоже убрана.
- Тесты `tests/test_salary_hours.py` (7) + e2e-смоук — зелёные. Док: раздел
  «Оплата часов по ролям», `docs/employee.md` (про источник часов).

### 2026-06-24 — журнал изменений графика (схема v4)
- Таблица `schedule_audit` (append-only) + методы `log_audit` / `get_audit` /
  `get_shift` / `get_day_off_request` в `ShiftsManager`. Миграция v3 -> v4
  additive (`CREATE TABLE IF NOT EXISTS`), бэкап `shifts.db.backup_v3`.
- Роуты смен/факта/выходных пишут автора (`current_user`) через `_audit`
  (best-effort, снимок «до» для delete/update). Новый `GET /api/schedule/audit`.
- Панель «История изменений» на `/schedule/edit` (ленивая, обновляется на reload)
  + компактная лента «Последние изменения» на `/schedule` (просмотр) с цветными
  точками по типу действия. Факт часов с `/schedule` тоже попадает в журнал —
  видно, кто проставил себе часы.
- Тесты `tests/test_schedule_audit.py` (7) + e2e-смоук (login -> CRUD смены ->
  чтение журнала, аноним 401) — зелёные.

### 2026-06-10 — редизайн: две страницы, факт часов, планы из весов
- Страница разделена: `/schedule` (просмотр, без денег) + `/schedule/edit`
  (рабочий стол). JS вынесен из inline (957 строк) в `static/js/schedule/`.
- Схема v3: `shifts.start_time`, `shifts.fact_minutes`, `locations.venue_key`,
  таблица `schedule_employees`. Миграции переведены с DROP (терял прод-данные
  при бампе версии) на additive + автоматический бэкап.
- Факт часов вводится барменом (модалка на просмотре) — единственный источник
  факта часов; агрегат «Часов» — только по факту.
- План дня read-only из daily_plans (веса дней), ручной ввод заморожен,
  фоллбэк-цепочка для непокрытых месяцев.
- Сводка месяца (план/факт/средняя/ожидаемая/выполнение %) с формулами
  в тултипах; синк факта за месяц одним запросом.
- Нагрузка по сотрудникам (смены/часы/«без часов», подсветка перекоса),
  предупреждения о дырах и конфликтах, пресеты времени кисти
  («с 14:00», «с 18:00»), реестр сотрудников с короткими метками.
- Убраны эмодзи из UI, цвета ролей берутся из БД (`roles.color`),
  число колонок сетки — из `locations` (не хардкод 4).

### 2026-03-27 — первая версия документа
Описывала модель с `employee_id`/`end_time`/`plan_revenue` на объекте Shift,
которой в коде никогда не было. Расхождение закрыто переписыванием 2026-06-10.
