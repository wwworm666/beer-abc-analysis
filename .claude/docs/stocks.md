# MOVED

Этот файл переехал в [docs/stocks.md](../../docs/stocks.md).

Документация проекта теперь живёт в корневом `docs/` (см. `.claude/INDEX.md`).
Этот stub можно удалить:

```powershell
git rm .claude/docs/stocks.md
```
