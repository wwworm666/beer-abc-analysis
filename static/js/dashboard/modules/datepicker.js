/**
 * УСТАРЕЛ. Выбор периода живёт в modules/period_controls.js (единая панель
 * «День|Неделя|Месяц|Год» + стрелки + календарь), арифметика дат —
 * в core/period_model.js.
 *
 * Файл оставлен намеренно пустой заглушкой, а не удалён: у пользователей неделями
 * висят открытые вкладки со старым dashboard.html, который импортирует этот путь
 * динамически. Пропавший файл дал бы 404 -> reject в Promise.all -> дашборд не
 * загрузился бы целиком (см. docs/lessons.md, «кэш браузера откатывает деплой»).
 *
 * Удалять можно, когда старые вкладки заведомо закрыты (ориентир — месяц после
 * выката 2026-08-11).
 */

/** Ничего не делает: раньше здесь создавался Flatpickr и применялся дефолтный период. */
export function init() {
    console.warn('[Datepicker] Модуль устарел, период выбирается в period_controls.js');
}

export const datepickerModule = { init };
