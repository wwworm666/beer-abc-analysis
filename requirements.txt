Flask==3.0.0
requests==2.31.0
pandas==2.2.3
numpy==1.26.2
python-dotenv==1.0.0
gunicorn==21.2.0
aiogram==3.4.1
aiohttp==3.9.1
playwright==1.49.1
# Эти пакеты импортируются на критичном пути (paramiko в remote_exec, portalocker
# в atomic write). Pin'аем lower bound + upper bound по major чтобы Docker rebuild
# не подтянул breaking changes из апстрима.
portalocker>=2.8,<3
openpyxl>=3.1,<4
python-dateutil>=2.8.2,<3
paramiko>=3.4.0,<4
Markdown>=3.5,<4