# 🐛 БАГ: BeerSharePercent отличается от фактических процентов на ~30%

## 📋 Описание проблемы

**Сообщение:**
> "Период анализа: 01.09.2025 — 30.09.2025, отличается от фактических процентов на 30. И ЭТО НЕ СВЯЗАНО С ПРОЧИМИ СПИКАМИ"

**Что это означает:**
```
Ожидаемо:  Brewlok IPA = 3.2% (на странице)
Фактично:  Brewlok IPA = ~33% (если вычислить вручную)

Разница: ≈ 30 процентных пункта!
```

---

## 🔍 Возможные причины (в порядке вероятности)

### 1️⃣ ОШИБКА ФИЛЬТРАЦИИ ПО ДАТЕ (НАИБОЛЕЕ ВЕРОЯТНО)

**Сценарий:**
- Frontend отправляет: `date_from="2025-09-01"`, `date_to="2025-09-30"` (СЕНТЯБРЬ)
- Backend рассчитывает за ДРУГОЙ период (например, последние 30 дней от текущей даты)
- Результаты за разные периоды → разные проценты

**Где проверить:**
```
📁 app.py (функция analyze_draft)
   ├─ Строка: date_from = data.get('date_from')
   ├─ Строка: date_to = data.get('date_to')
   └─ ❓ Используются ли эти значения или берутся свои?
```

**Код для проверки:**
```python
# В app.py функция analyze_draft()

# ❌ НЕПРАВИЛЬНО (игнорирует date_from/date_to):
date_to = datetime.now().strftime("%Y-%m-%d")
date_from = (datetime.now() - timedelta(days=days)).strftime("%Y-%m-%d")

# ✅ ПРАВИЛЬНО (использует переданные даты):
if date_from and date_to:
    # используем переданные даты
else:
    # используем days
```

---

### 2️⃣ ОШИБКА АГРЕГИРОВАНИЯ (ВЕРОЯТНО)

**Сценарий:**
- При расчёте BeerSharePercent неправильно определяется denominator
- Например, вместо суммы всех пив, берётся сумма только видимых

**Где проверить:**
```
📁 core/draft_analysis.py (функция get_beer_summary)
   └─ Строка ~165: total_liters = summary['TotalLiters'].sum()
      └─ ❓ Это сумма ДО или ПОСЛЕ фильтрации?
```

**Проблема:**
```python
# ❌ НЕПРАВИЛЬНО (если summary уже отфильтрована):
summary = summary[summary['TotalLiters'] > 10]  # фильтруем
total_liters = summary['TotalLiters'].sum()      # sum ТОЛЬКО отфильтрованных!
summary['BeerSharePercent'] = (summary['TotalLiters'] / total_liters * 100)
# Результат: проценты будут больше! (100% от subset, а не от всех)

# ✅ ПРАВИЛЬНО:
total_liters_all = df_original['TotalLiters'].sum()  # sum ВСЕх
summary = summary[summary['TotalLiters'] > 10]       # фильтруем
summary['BeerSharePercent'] = (summary['TotalLiters'] / total_liters_all * 100)
```

---

### 3️⃣ ФИЛЬТРАЦИЯ ПО КАТЕГОРИЯМ (МЕНЕЕ ВЕРОЯТНО)

**Сценарий:**
- Исключаются определённые категории пива
- Это меняет sum (denominator)
- Все проценты становятся больше

**Где проверить:**
```
📁 app.py (функция analyze_draft)
   └─ Есть ли строки типа:
      • df = df[df['DishGroup'] == 'Напитки Розлив']
      • df = df[~df['BeerName'].isin(excluded_beers)]
      • Другие фильтры?
```

---

### 4️⃣ ДУБЛИРУЮЩИЕСЯ ЗАПИСИ В OLAP (МЕНЕЕ ВЕРОЯТНО)

**Сценарий:**
- OLAP возвращает одну транзакцию дважды
- Объём пива удваивается
- При расчёте проценты смещаются

**Диагностика:**
```python
# Проверить дубликаты:
df_raw = pd.DataFrame(report_data['data'])
duplicates = df_raw[df_raw.duplicated(subset=['DishName', 'OpenDate.Typed', 'Store.Name'])]
if not duplicates.empty:
    print(f"НАЙДЕНО {len(duplicates)} дубликатов!")
```

---

### 5️⃣ НЕПРАВИЛЬНАЯ ИНТЕРПРЕТАЦИЯ ДАТЕ (МЕНЕЕ ВЕРОЯТНО)

**Сценарий:**
- Дата от/до интерпретируется неправильно
- Например, `2025-09-30` включает только до 00:00
- Последний день данных пропускается

**Где проверить:**
```
📁 core/olap_reports.py (функция get_draft_sales_report)
   └─ Как передаются даты в OLAP запрос?
```

---

## 🔧 КАК ОТСЛЕДИТЬ ПРОБЛЕМУ (пошагово)

### ШАГ 1: Получить raw данные из OLAP

```python
from core.olap_reports import OlapReports
from core.draft_analysis import DraftAnalysis
import pandas as pd

olap = OlapReports()
olap.connect()

# Запросить ТОЧНЫЕ данные за СЕНТЯБРЬ
report_data = olap.get_draft_sales_report('2025-09-01', '2025-09-30', None)
olap.disconnect()

# Преобразовать в DataFrame
df_raw = pd.DataFrame(report_data['data'])
df_raw['DishAmountInt'] = pd.to_numeric(df_raw['DishAmountInt'], errors='coerce')
df_raw['OpenDate'] = pd.to_datetime(df_raw['OpenDate.Typed'])

print(f"Всего записей в OLAP: {len(df_raw)}")
print(f"Дата от: {df_raw['OpenDate'].min()}")
print(f"Дата до: {df_raw['OpenDate'].max()}")
```

### ШАГ 2: Проверить объём в raw данных

```python
# Подсчитать объём в сыром виде
analyzer = DraftAnalysis(df_raw)
df_prepared = analyzer.prepare_draft_data()

print(f"\nОбщий объём в raw data: {df_prepared['VolumeInLiters'].sum()} L")

# Проверить по пивам
by_beer = df_prepared.groupby('BeerName').agg({
    'VolumeInLiters': 'sum',
    'DishAmountInt': 'sum'
}).reset_index()

by_beer['Share'] = (by_beer['VolumeInLiters'] / df_prepared['VolumeInLiters'].sum() * 100)
by_beer = by_beer.sort_values('VolumeInLiters', ascending=False)

print("\nПо пивам (raw):")
print(by_beer[['BeerName', 'VolumeInLiters', 'Share']].head(10).to_string())
```

### ШАГ 3: Сравнить с get_beer_summary()

```python
# Получить summary (как это делает API)
summary = analyzer.get_beer_summary(None)  # None = все бары

print("\nПо пивам (summary):")
print(summary[['BeerName', 'TotalLiters', 'BeerSharePercent']].head(10).to_string())

# СРАВНИТЬ
raw_total = by_beer['VolumeInLiters'].sum()
summary_total = summary['TotalLiters'].sum()

print(f"\nРазница в total:")
print(f"  raw data: {raw_total} L")
print(f"  summary:  {summary_total} L")
print(f"  Ratio:    {summary_total / raw_total:.2f}x")
```

### ШАГ 4: Проверить API endpoint

```bash
# Запрос к API с конкретными датами
curl -X POST http://localhost:5000/api/draft-analyze \
  -H "Content-Type: application/json" \
  -d '{
    "bar": null,
    "days": 30,
    "date_from": "2025-09-01",
    "date_to": "2025-09-30"
  }' | python3 -m json.tool | head -50
```

### ШАГ 5: Проверить frontend запрос

```javascript
// Открыть F12 (Developer Tools) в браузере
// Вкладка Network
// Фильтр: XHR (или Fetch)
// Нажать кнопку "Анализ" на странице "Проливы"
// Посмотреть запрос в Network:

// POST /api/draft-analyze
// Body:
{
  "bar": null,
  "days": 30,
  "date_from": "2025-09-01",  // ← ЭТИ ЗНАЧЕНИЯ!
  "date_to": "2025-09-30"      // ← СОВПАДАЮТ ЛИ?
}
```

---

## 🎯 НАИБОЛЕЕ ВЕРОЯТНАЯ ОШИБКА

На основе описания ("отличается на 30%"), я подозреваю:

### ❌ ОШИБКА 1: Игнорирование date_from/date_to в API

**Месторасположение:** `app.py` функция `analyze_draft()`

**Текущий код (НЕПРАВИЛЬНЫЙ):**
```python
# Игнорирует переданные даты!
if date_from and date_to:
    print(f"Period: {date_from} - {date_to}")
else:
    # ВСЕГДА используется это!
    date_to = datetime.now().strftime("%Y-%m-%d")
    date_from = (datetime.now() - timedelta(days=days)).strftime("%Y-%m-%d")
    print(f"Computed dates: {date_from} - {date_to}")
```

**Проблема:**
- Frontend отправляет: `date_from="2025-09-01"`, `date_to="2025-09-30"`
- Backend проверяет: `if date_from and date_to:` ← TRUE
- Но потом всё равно пересчитывает даты! ← БАГИ

**Решение:**
```python
# ✅ ПРАВИЛЬНЫЙ КОД:
if date_from and date_to:
    # Использовать переданные даты
    print(f"Period: {date_from} - {date_to}")
    # НЕ пересчитывать!
else:
    # Только если даты не переданы
    date_to = datetime.now().strftime("%Y-%m-%d")
    date_from = (datetime.now() - timedelta(days=days)).strftime("%Y-%m-%d")
```

---

### ❌ ОШИБКА 2: Неправильная группировка при агрегировании

**Месторасположение:** `core/draft_analysis.py` функция `get_beer_summary()`

**Возможная проблема:**
```python
# Может быть группируется не только по BeerName?
summary = df.groupby(['BeerName', 'Store.Name']).agg({...})

# Если потом считать Share, нужно учесть, что это ДВА уровня группировки!
total_liters = summary['TotalLiters'].sum()  # ← Это контекст-зависимо!
```

---

## ✅ КОМАНДА ДЛЯ БЫСТРОЙ ПРОВЕРКИ

Запустите этот скрипт:

```bash
python3 debug_beer_share_calculation.py
```

Он выведет все возможные варианты расчётов и покажет, какой результат даёт каждый.

---

## 📌 ЧЕК-ЛИСТ ДЛЯ ПОИСКА

- [ ] Проверить, используются ли date_from/date_to в `analyze_draft()`
- [ ] Проверить, не переопределяются ли даты внутри функции
- [ ] Проверить логику группировки в `get_beer_summary()`
- [ ] Проверить, нет ли фильтров, которые меняют denominator
- [ ] Запустить debug скрипт и сравнить результаты
- [ ] Проверить в Browser DevTools, какие даты отправляются в API
- [ ] Проверить raw данные из OLAP за выбранный период

---

## 🔗 Связанные файлы для проверки

1. `app.py` → функция `analyze_draft()` (строки ~410-750)
   - Обработка date_from/date_to
   - Вызов olap.get_draft_sales_report()

2. `core/draft_analysis.py` → функция `get_beer_summary()` (строки ~99-178)
   - Расчёт BeerSharePercent
   - Логика группировки

3. `core/olap_reports.py` → функция `get_draft_sales_report()` (строки ~70+)
   - Передача дат в OLAP запрос

4. `templates/draft.html`
   - Какие параметры отправляются при клике "Анализ"

---

## 📞 Следующий шаг

Пожалуйста:
1. Запустите `debug_beer_share_calculation.py`
2. Покажите результат сравнения raw vs summary
3. Проверьте в DevTools, какие даты отправляются в API
4. Я помогу найти и исправить точное место ошибки!

