# 📚 Обзор документации системы

Полное руководство по пониманию и работе с системой маппирования кегов и анализом разливного пива.

---

## 📖 Основные документы

### 1. **MAPPING_SYSTEM_GUIDE.md** - 🗂️ Архитектура мэппинга
   - **Для кого:** Разработчики, которые работают с системой мэппинга
   - **Содержит:**
     - Полное описание системы KEG ↔ DISH
     - Структуру всех файлов мэппинга
     - Процессы обновления и импорта
     - Примеры использования в коде
     - Решение проблем
     - Форматы файлов (CSV, JSON, Python)

   **Читайте если:**
   - Нужно добавить новое пиво в систему
   - Нужно обновить мэппинг
   - Хотите понять, как работает мэппинг
   - Нужно исправить ошибку в сопоставлении

---

### 2. **MAPPING_DATA_FLOW.md** - 🔄 Поток данных и синхронизация
   - **Для кого:** Разработчики, бизнес-аналитики, тестировщики
   - **Содержит:**
     - Полный жизненный цикл продажи пива (от клиента к аналитике)
     - Пошаговый разбор синхронизации
     - Примеры реальных данных
     - Матрица возможных проблем и решений
     - Чек-лист для проверки синхронизации
     - Сценарии нарушения синхронизации

   **Читайте если:**
   - Хотите понять, как данные движутся через систему
   - Нужно отладить проблему с данными
   - Хотите проверить, синхронизирована ли система
   - Нужно обучить кого-то новичка

---

### 3. **SYNC_FLOW_VISUAL.md** - 📊 Визуальные диаграммы
   - **Для кого:** Все, кто лучше понимает графики и диаграммы
   - **Содержит:**
     - 6 ASCII диаграмм процессов
     - Жизненный цикл пива (от заказа к отчёту)
     - Архитектура мэппинга (файлы и структуры)
     - Процесс нормализации имён
     - Жизненный цикл обновления мэппинга
     - Структуры данных в памяти
     - Полная синхронизация между системами

   **Читайте если:**
   - Вы визуал и лучше понимаете диаграммы
   - Нужно показать процесс руководству
   - Хотите увидеть "большую картину"
   - Нужно создать презентацию

---

## 🔧 Практические гайды

### Как добавить новое пиво?

```bash
# 1. Обнаружить новое пиво в продажах
python utils/check_unmapped_dishes.py

# 2. Результат → NEW_DISHES_TO_ADD.csv
# Открыть файл и заполнить KEG_NAME для новых блюд

# 3. Автоматически сопоставить
python utils/auto_add_new_dishes.py

# 4. Переимпортировать мэппинг
python utils/import_final_mapping.py

# 5. Перезагрузить приложение
python app.py
```

**Подробнее:** см. MAPPING_SYSTEM_GUIDE.md → "Цикл обновления мэппинга"

---

### Как исправить рассинхрон?

**Проблема:** Новое пиво не в отчёте

```bash
# 1. Проверить, есть ли в мэппинге
grep "BeerName" mapping/keg_mapping.py

# 2. Если нет → запустить обновление
python utils/check_unmapped_dishes.py
python utils/auto_add_new_dishes.py
python utils/import_final_mapping.py

# 3. Перезагрузить приложение
python app.py
```

**Подробнее:** см. MAPPING_DATA_FLOW.md → "Сценарий 1-3" или MAPPING_SYSTEM_GUIDE.md → "Решение проблем"

---

### Как проверить синхронизацию?

**Чек-лист из MAPPING_DATA_FLOW.md:**

```
[ ] 1. CSV исходный файл актуален?
[ ] 2. Все новые блюда в мэппинге?
[ ] 3. KEG_TO_DISH_MAPPING актуален?
[ ] 4. JSON мэппинги синхронизированы?
[ ] 5. Приложение перезагружено?
```

---

## 📋 Дополнительные ресурсы

### Основные файлы проекта

| Файл | Назначение |
|------|-----------|
| `mapping/keg_mapping.py` | Production mapping (140 кегов) |
| `data/keg_mapping.json` | JSON версия для API |
| `data/dish_to_keg_mapping.json` | Обратный индекс |
| `utils/import_final_mapping.py` | Основной скрипт импорта |
| `utils/check_unmapped_dishes.py` | Проверка новых блюд |
| `utils/auto_add_new_dishes.py` | Fuzzy matching |
| `core/draft_analysis.py` | Анализ продаж разливного пива |
| `core/waiter_analysis.py` | Анализ по официантам |

### Форматы данных

**CSV исходный:**
```
KEG_NAME | DISH_NAME | PREFIX | STATUS
КЕГ Brewlok IPA 20л | Brewlok IPA | | VERIFIED
```

**Python (dict):**
```python
KEG_TO_DISH_MAPPING = {
    'КЕГ Brewlok IPA 20л': ['Brewlok IPA', 'ВО Brewlok IPA']
}
```

**JSON (forward):**
```json
{"КЕГ Brewlok IPA 20л": ["Brewlok IPA", "ВО Brewlok IPA"]}
```

**JSON (reverse):**
```json
{"Brewlok IPA": "КЕГ Brewlok IPA 20л"}
```

---

## 🐛 Исправленные ошибки

### Ошибка парсинга названий пива

**Проблема:** Позиции вроде `"ХБ Октоберфест (1,0) с собой"` не парсились

**Решение:** Обновлен regex в `extract_beer_info()`
- **Файлы:** `core/draft_analysis.py`, `core/waiter_analysis.py`
- **Старый regex:** `r'^(.+?)\s*\((\d+[,\.]\d+)\)\s*$'` (строгий)
- **Новый regex:** `r'\((\d+[,\.]\d+)\)'` (гибкий)
- **Результат:** Все позиции теперь правильно парсятся ✓

---

## 📊 Статистика системы

- **Активные кеги:** ~140
- **Блюда:** Сотни (1 кег → несколько блюд)
- **Префиксы:** НЗ, ВО, ТЦ
- **Размеры кегов:** 20л, 30л, 50л
- **Последнее обновление:** 2025-10-18

---

## 🗺️ Навигация по документам

```
DOCUMENTATION_OVERVIEW.md (ВЫ ЗДЕСЬ)
    ├─ MAPPING_SYSTEM_GUIDE.md       (Архитектура)
    ├─ MAPPING_DATA_FLOW.md          (Процессы и сценарии)
    └─ SYNC_FLOW_VISUAL.md           (Диаграммы)
```

**Быстрая навигация:**

| Вопрос | Ответ в |
|--------|----------|
| Как работает мэппинг? | MAPPING_SYSTEM_GUIDE.md |
| Как данные движутся? | MAPPING_DATA_FLOW.md, SYNC_FLOW_VISUAL.md |
| Как добавить пиво? | MAPPING_SYSTEM_GUIDE.md → "Цикл обновления" |
| Как исправить ошибку? | MAPPING_SYSTEM_GUIDE.md → "Решение проблем" |
| Что делать при рассинхроне? | MAPPING_DATA_FLOW.md → "Возможные проблемы" |
| Как проверить синхронизацию? | MAPPING_DATA_FLOW.md → "Чек-лист" |

---

## 💡 Ключевые концепции

### KEG (Кег)
**Определение:** Физический товар в инвентаре iiko
- Пример: `КЕГ Brewlok IPA 20 л`
- Атрибуты: название + объём (20л, 30л, 50л)
- Используется: для учёта инвентаря, затрат, поставок

### DISH (Блюдо)
**Определение:** Позиция в меню/чеке POS
- Пример: `Brewlok IPA (0,5)`, `ВО Brewlok IPA (0,5)`
- Атрибуты: название пива + размер порции + префикс
- Используется: для записи продаж, чеков, отчётов

### MAPPING (Мэппинг)
**Определение:** Двусторонний индекс между Кегами и Блюдами
- Forward: `КЕГ → [Блюдо1, Блюдо2, ...]`
- Reverse: `Блюдо → КЕГ`
- Назначение: связать инвентарь с продажами

### СИНХРОНИЗАЦИЯ
**Определение:** Процесс поддержания актуальности мэппинга
- Шаг 1: Обнаружить новые блюда (check_unmapped_dishes.py)
- Шаг 2: Сопоставить кеги (auto_add_new_dishes.py)
- Шаг 3: Переимпортировать (import_final_mapping.py)
- Шаг 4: Перезагрузить приложение

---

## 🎯 Резюме

Эти документы предоставляют полное понимание того, как:

1. **Система мэппинга** соединяет кеги инвентаря с блюдами меню
2. **Данные** движутся от клиента через iiko к аналитике
3. **Названия** пива нормализуются и сопоставляются
4. **Синхронизация** поддерживается автоматически
5. **Проблемы** обнаруживаются и исправляются

**Результат:** Правильные расчёты доли пива (%), объёмов и всей аналитики на странице "Проливы" ✓

---

## 📞 Контакты

Вопросы по документации? Смотрите соответствующий файл:
- Архитектура → MAPPING_SYSTEM_GUIDE.md
- Процессы → MAPPING_DATA_FLOW.md
- Диаграммы → SYNC_FLOW_VISUAL.md

Вопросы по коду? Смотрите исходники:
- Анализ → `core/draft_analysis.py`
- Официанты → `core/waiter_analysis.py`
- API → `app.py`
